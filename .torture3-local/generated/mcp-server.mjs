// Generated by supawatch. Do not edit.
import { McpServer } from "@modelcontextprotocol/sdk/server/mcp.js";
import { z } from "zod";
import { public_USERSRow } from "./zod/public.USERS.mjs";
import { public_UsersRow } from "./zod/public.Users.mjs";
import { public_auditedRow } from "./zod/public.audited.mjs";
import { public_chain_1Row } from "./zod/public.chain_1.mjs";
import { public_chain_10Row } from "./zod/public.chain_10.mjs";
import { public_chain_11Row } from "./zod/public.chain_11.mjs";
import { public_chain_12Row } from "./zod/public.chain_12.mjs";
import { public_chain_13Row } from "./zod/public.chain_13.mjs";
import { public_chain_14Row } from "./zod/public.chain_14.mjs";
import { public_chain_15Row } from "./zod/public.chain_15.mjs";
import { public_chain_16Row } from "./zod/public.chain_16.mjs";
import { public_chain_17Row } from "./zod/public.chain_17.mjs";
import { public_chain_18Row } from "./zod/public.chain_18.mjs";
import { public_chain_19Row } from "./zod/public.chain_19.mjs";
import { public_chain_2Row } from "./zod/public.chain_2.mjs";
import { public_chain_20Row } from "./zod/public.chain_20.mjs";
import { public_chain_21Row } from "./zod/public.chain_21.mjs";
import { public_chain_22Row } from "./zod/public.chain_22.mjs";
import { public_chain_23Row } from "./zod/public.chain_23.mjs";
import { public_chain_24Row } from "./zod/public.chain_24.mjs";
import { public_chain_25Row } from "./zod/public.chain_25.mjs";
import { public_chain_26Row } from "./zod/public.chain_26.mjs";
import { public_chain_27Row } from "./zod/public.chain_27.mjs";
import { public_chain_28Row } from "./zod/public.chain_28.mjs";
import { public_chain_29Row } from "./zod/public.chain_29.mjs";
import { public_chain_3Row } from "./zod/public.chain_3.mjs";
import { public_chain_30Row } from "./zod/public.chain_30.mjs";
import { public_chain_4Row } from "./zod/public.chain_4.mjs";
import { public_chain_5Row } from "./zod/public.chain_5.mjs";
import { public_chain_6Row } from "./zod/public.chain_6.mjs";
import { public_chain_7Row } from "./zod/public.chain_7.mjs";
import { public_chain_8Row } from "./zod/public.chain_8.mjs";
import { public_chain_9Row } from "./zod/public.chain_9.mjs";
import { public_clockedRow } from "./zod/public.clocked.mjs";
import { public_colossusRow } from "./zod/public.colossus.mjs";
import { public_defaultRow } from "./zod/public.default.mjs";
import { public_extremesRow } from "./zod/public.extremes.mjs";
import { public_gen_childrenRow } from "./zod/public.gen_children.mjs";
import { public_gen_parentsRow } from "./zod/public.gen_parents.mjs";
import { public_kiloRow } from "./zod/public.kilo.mjs";
import { aux_leavesRow } from "./zod/aux.leaves.mjs";
import { public_newRow } from "./zod/public.new.mjs";
import { ___panicRow } from "./zod/😱.panic.mjs";
import { public_reserved_colsRow } from "./zod/public.reserved_cols.mjs";
import { public_rootsRow } from "./zod/public.roots.mjs";
import { public_share_aRow } from "./zod/public.share_a.mjs";
import { public_share_bRow } from "./zod/public.share_b.mjs";
import { public_stampedRow } from "./zod/public.stamped.mjs";
import { public_swarm_1Row } from "./zod/public.swarm_1.mjs";
import { public_swarm_10Row } from "./zod/public.swarm_10.mjs";
import { public_swarm_100Row } from "./zod/public.swarm_100.mjs";
import { public_swarm_101Row } from "./zod/public.swarm_101.mjs";
import { public_swarm_102Row } from "./zod/public.swarm_102.mjs";
import { public_swarm_103Row } from "./zod/public.swarm_103.mjs";
import { public_swarm_104Row } from "./zod/public.swarm_104.mjs";
import { public_swarm_105Row } from "./zod/public.swarm_105.mjs";
import { public_swarm_106Row } from "./zod/public.swarm_106.mjs";
import { public_swarm_107Row } from "./zod/public.swarm_107.mjs";
import { public_swarm_108Row } from "./zod/public.swarm_108.mjs";
import { public_swarm_109Row } from "./zod/public.swarm_109.mjs";
import { public_swarm_11Row } from "./zod/public.swarm_11.mjs";
import { public_swarm_110Row } from "./zod/public.swarm_110.mjs";
import { public_swarm_111Row } from "./zod/public.swarm_111.mjs";
import { public_swarm_112Row } from "./zod/public.swarm_112.mjs";
import { public_swarm_113Row } from "./zod/public.swarm_113.mjs";
import { public_swarm_114Row } from "./zod/public.swarm_114.mjs";
import { public_swarm_115Row } from "./zod/public.swarm_115.mjs";
import { public_swarm_116Row } from "./zod/public.swarm_116.mjs";
import { public_swarm_117Row } from "./zod/public.swarm_117.mjs";
import { public_swarm_118Row } from "./zod/public.swarm_118.mjs";
import { public_swarm_119Row } from "./zod/public.swarm_119.mjs";
import { public_swarm_12Row } from "./zod/public.swarm_12.mjs";
import { public_swarm_120Row } from "./zod/public.swarm_120.mjs";
import { public_swarm_121Row } from "./zod/public.swarm_121.mjs";
import { public_swarm_122Row } from "./zod/public.swarm_122.mjs";
import { public_swarm_123Row } from "./zod/public.swarm_123.mjs";
import { public_swarm_124Row } from "./zod/public.swarm_124.mjs";
import { public_swarm_125Row } from "./zod/public.swarm_125.mjs";
import { public_swarm_126Row } from "./zod/public.swarm_126.mjs";
import { public_swarm_127Row } from "./zod/public.swarm_127.mjs";
import { public_swarm_128Row } from "./zod/public.swarm_128.mjs";
import { public_swarm_129Row } from "./zod/public.swarm_129.mjs";
import { public_swarm_13Row } from "./zod/public.swarm_13.mjs";
import { public_swarm_130Row } from "./zod/public.swarm_130.mjs";
import { public_swarm_131Row } from "./zod/public.swarm_131.mjs";
import { public_swarm_132Row } from "./zod/public.swarm_132.mjs";
import { public_swarm_133Row } from "./zod/public.swarm_133.mjs";
import { public_swarm_134Row } from "./zod/public.swarm_134.mjs";
import { public_swarm_135Row } from "./zod/public.swarm_135.mjs";
import { public_swarm_136Row } from "./zod/public.swarm_136.mjs";
import { public_swarm_137Row } from "./zod/public.swarm_137.mjs";
import { public_swarm_138Row } from "./zod/public.swarm_138.mjs";
import { public_swarm_139Row } from "./zod/public.swarm_139.mjs";
import { public_swarm_14Row } from "./zod/public.swarm_14.mjs";
import { public_swarm_140Row } from "./zod/public.swarm_140.mjs";
import { public_swarm_141Row } from "./zod/public.swarm_141.mjs";
import { public_swarm_142Row } from "./zod/public.swarm_142.mjs";
import { public_swarm_143Row } from "./zod/public.swarm_143.mjs";
import { public_swarm_144Row } from "./zod/public.swarm_144.mjs";
import { public_swarm_145Row } from "./zod/public.swarm_145.mjs";
import { public_swarm_146Row } from "./zod/public.swarm_146.mjs";
import { public_swarm_147Row } from "./zod/public.swarm_147.mjs";
import { public_swarm_148Row } from "./zod/public.swarm_148.mjs";
import { public_swarm_149Row } from "./zod/public.swarm_149.mjs";
import { public_swarm_15Row } from "./zod/public.swarm_15.mjs";
import { public_swarm_150Row } from "./zod/public.swarm_150.mjs";
import { public_swarm_151Row } from "./zod/public.swarm_151.mjs";
import { public_swarm_152Row } from "./zod/public.swarm_152.mjs";
import { public_swarm_153Row } from "./zod/public.swarm_153.mjs";
import { public_swarm_154Row } from "./zod/public.swarm_154.mjs";
import { public_swarm_155Row } from "./zod/public.swarm_155.mjs";
import { public_swarm_156Row } from "./zod/public.swarm_156.mjs";
import { public_swarm_157Row } from "./zod/public.swarm_157.mjs";
import { public_swarm_158Row } from "./zod/public.swarm_158.mjs";
import { public_swarm_159Row } from "./zod/public.swarm_159.mjs";
import { public_swarm_16Row } from "./zod/public.swarm_16.mjs";
import { public_swarm_160Row } from "./zod/public.swarm_160.mjs";
import { public_swarm_161Row } from "./zod/public.swarm_161.mjs";
import { public_swarm_162Row } from "./zod/public.swarm_162.mjs";
import { public_swarm_163Row } from "./zod/public.swarm_163.mjs";
import { public_swarm_164Row } from "./zod/public.swarm_164.mjs";
import { public_swarm_165Row } from "./zod/public.swarm_165.mjs";
import { public_swarm_166Row } from "./zod/public.swarm_166.mjs";
import { public_swarm_167Row } from "./zod/public.swarm_167.mjs";
import { public_swarm_168Row } from "./zod/public.swarm_168.mjs";
import { public_swarm_169Row } from "./zod/public.swarm_169.mjs";
import { public_swarm_17Row } from "./zod/public.swarm_17.mjs";
import { public_swarm_170Row } from "./zod/public.swarm_170.mjs";
import { public_swarm_171Row } from "./zod/public.swarm_171.mjs";
import { public_swarm_172Row } from "./zod/public.swarm_172.mjs";
import { public_swarm_173Row } from "./zod/public.swarm_173.mjs";
import { public_swarm_174Row } from "./zod/public.swarm_174.mjs";
import { public_swarm_175Row } from "./zod/public.swarm_175.mjs";
import { public_swarm_176Row } from "./zod/public.swarm_176.mjs";
import { public_swarm_177Row } from "./zod/public.swarm_177.mjs";
import { public_swarm_178Row } from "./zod/public.swarm_178.mjs";
import { public_swarm_179Row } from "./zod/public.swarm_179.mjs";
import { public_swarm_18Row } from "./zod/public.swarm_18.mjs";
import { public_swarm_180Row } from "./zod/public.swarm_180.mjs";
import { public_swarm_181Row } from "./zod/public.swarm_181.mjs";
import { public_swarm_182Row } from "./zod/public.swarm_182.mjs";
import { public_swarm_183Row } from "./zod/public.swarm_183.mjs";
import { public_swarm_184Row } from "./zod/public.swarm_184.mjs";
import { public_swarm_185Row } from "./zod/public.swarm_185.mjs";
import { public_swarm_186Row } from "./zod/public.swarm_186.mjs";
import { public_swarm_187Row } from "./zod/public.swarm_187.mjs";
import { public_swarm_188Row } from "./zod/public.swarm_188.mjs";
import { public_swarm_189Row } from "./zod/public.swarm_189.mjs";
import { public_swarm_19Row } from "./zod/public.swarm_19.mjs";
import { public_swarm_190Row } from "./zod/public.swarm_190.mjs";
import { public_swarm_191Row } from "./zod/public.swarm_191.mjs";
import { public_swarm_192Row } from "./zod/public.swarm_192.mjs";
import { public_swarm_193Row } from "./zod/public.swarm_193.mjs";
import { public_swarm_194Row } from "./zod/public.swarm_194.mjs";
import { public_swarm_195Row } from "./zod/public.swarm_195.mjs";
import { public_swarm_196Row } from "./zod/public.swarm_196.mjs";
import { public_swarm_197Row } from "./zod/public.swarm_197.mjs";
import { public_swarm_198Row } from "./zod/public.swarm_198.mjs";
import { public_swarm_199Row } from "./zod/public.swarm_199.mjs";
import { public_swarm_2Row } from "./zod/public.swarm_2.mjs";
import { public_swarm_20Row } from "./zod/public.swarm_20.mjs";
import { public_swarm_200Row } from "./zod/public.swarm_200.mjs";
import { public_swarm_201Row } from "./zod/public.swarm_201.mjs";
import { public_swarm_202Row } from "./zod/public.swarm_202.mjs";
import { public_swarm_203Row } from "./zod/public.swarm_203.mjs";
import { public_swarm_204Row } from "./zod/public.swarm_204.mjs";
import { public_swarm_205Row } from "./zod/public.swarm_205.mjs";
import { public_swarm_206Row } from "./zod/public.swarm_206.mjs";
import { public_swarm_207Row } from "./zod/public.swarm_207.mjs";
import { public_swarm_208Row } from "./zod/public.swarm_208.mjs";
import { public_swarm_209Row } from "./zod/public.swarm_209.mjs";
import { public_swarm_21Row } from "./zod/public.swarm_21.mjs";
import { public_swarm_210Row } from "./zod/public.swarm_210.mjs";
import { public_swarm_211Row } from "./zod/public.swarm_211.mjs";
import { public_swarm_212Row } from "./zod/public.swarm_212.mjs";
import { public_swarm_213Row } from "./zod/public.swarm_213.mjs";
import { public_swarm_214Row } from "./zod/public.swarm_214.mjs";
import { public_swarm_215Row } from "./zod/public.swarm_215.mjs";
import { public_swarm_216Row } from "./zod/public.swarm_216.mjs";
import { public_swarm_217Row } from "./zod/public.swarm_217.mjs";
import { public_swarm_218Row } from "./zod/public.swarm_218.mjs";
import { public_swarm_219Row } from "./zod/public.swarm_219.mjs";
import { public_swarm_22Row } from "./zod/public.swarm_22.mjs";
import { public_swarm_220Row } from "./zod/public.swarm_220.mjs";
import { public_swarm_221Row } from "./zod/public.swarm_221.mjs";
import { public_swarm_222Row } from "./zod/public.swarm_222.mjs";
import { public_swarm_223Row } from "./zod/public.swarm_223.mjs";
import { public_swarm_224Row } from "./zod/public.swarm_224.mjs";
import { public_swarm_225Row } from "./zod/public.swarm_225.mjs";
import { public_swarm_226Row } from "./zod/public.swarm_226.mjs";
import { public_swarm_227Row } from "./zod/public.swarm_227.mjs";
import { public_swarm_228Row } from "./zod/public.swarm_228.mjs";
import { public_swarm_229Row } from "./zod/public.swarm_229.mjs";
import { public_swarm_23Row } from "./zod/public.swarm_23.mjs";
import { public_swarm_230Row } from "./zod/public.swarm_230.mjs";
import { public_swarm_231Row } from "./zod/public.swarm_231.mjs";
import { public_swarm_232Row } from "./zod/public.swarm_232.mjs";
import { public_swarm_233Row } from "./zod/public.swarm_233.mjs";
import { public_swarm_234Row } from "./zod/public.swarm_234.mjs";
import { public_swarm_235Row } from "./zod/public.swarm_235.mjs";
import { public_swarm_236Row } from "./zod/public.swarm_236.mjs";
import { public_swarm_237Row } from "./zod/public.swarm_237.mjs";
import { public_swarm_238Row } from "./zod/public.swarm_238.mjs";
import { public_swarm_239Row } from "./zod/public.swarm_239.mjs";
import { public_swarm_24Row } from "./zod/public.swarm_24.mjs";
import { public_swarm_240Row } from "./zod/public.swarm_240.mjs";
import { public_swarm_241Row } from "./zod/public.swarm_241.mjs";
import { public_swarm_242Row } from "./zod/public.swarm_242.mjs";
import { public_swarm_243Row } from "./zod/public.swarm_243.mjs";
import { public_swarm_244Row } from "./zod/public.swarm_244.mjs";
import { public_swarm_245Row } from "./zod/public.swarm_245.mjs";
import { public_swarm_246Row } from "./zod/public.swarm_246.mjs";
import { public_swarm_247Row } from "./zod/public.swarm_247.mjs";
import { public_swarm_248Row } from "./zod/public.swarm_248.mjs";
import { public_swarm_249Row } from "./zod/public.swarm_249.mjs";
import { public_swarm_25Row } from "./zod/public.swarm_25.mjs";
import { public_swarm_250Row } from "./zod/public.swarm_250.mjs";
import { public_swarm_251Row } from "./zod/public.swarm_251.mjs";
import { public_swarm_252Row } from "./zod/public.swarm_252.mjs";
import { public_swarm_253Row } from "./zod/public.swarm_253.mjs";
import { public_swarm_254Row } from "./zod/public.swarm_254.mjs";
import { public_swarm_255Row } from "./zod/public.swarm_255.mjs";
import { public_swarm_256Row } from "./zod/public.swarm_256.mjs";
import { public_swarm_257Row } from "./zod/public.swarm_257.mjs";
import { public_swarm_258Row } from "./zod/public.swarm_258.mjs";
import { public_swarm_259Row } from "./zod/public.swarm_259.mjs";
import { public_swarm_26Row } from "./zod/public.swarm_26.mjs";
import { public_swarm_260Row } from "./zod/public.swarm_260.mjs";
import { public_swarm_261Row } from "./zod/public.swarm_261.mjs";
import { public_swarm_262Row } from "./zod/public.swarm_262.mjs";
import { public_swarm_263Row } from "./zod/public.swarm_263.mjs";
import { public_swarm_264Row } from "./zod/public.swarm_264.mjs";
import { public_swarm_265Row } from "./zod/public.swarm_265.mjs";
import { public_swarm_266Row } from "./zod/public.swarm_266.mjs";
import { public_swarm_267Row } from "./zod/public.swarm_267.mjs";
import { public_swarm_268Row } from "./zod/public.swarm_268.mjs";
import { public_swarm_269Row } from "./zod/public.swarm_269.mjs";
import { public_swarm_27Row } from "./zod/public.swarm_27.mjs";
import { public_swarm_270Row } from "./zod/public.swarm_270.mjs";
import { public_swarm_271Row } from "./zod/public.swarm_271.mjs";
import { public_swarm_272Row } from "./zod/public.swarm_272.mjs";
import { public_swarm_273Row } from "./zod/public.swarm_273.mjs";
import { public_swarm_274Row } from "./zod/public.swarm_274.mjs";
import { public_swarm_275Row } from "./zod/public.swarm_275.mjs";
import { public_swarm_276Row } from "./zod/public.swarm_276.mjs";
import { public_swarm_277Row } from "./zod/public.swarm_277.mjs";
import { public_swarm_278Row } from "./zod/public.swarm_278.mjs";
import { public_swarm_279Row } from "./zod/public.swarm_279.mjs";
import { public_swarm_28Row } from "./zod/public.swarm_28.mjs";
import { public_swarm_280Row } from "./zod/public.swarm_280.mjs";
import { public_swarm_281Row } from "./zod/public.swarm_281.mjs";
import { public_swarm_282Row } from "./zod/public.swarm_282.mjs";
import { public_swarm_283Row } from "./zod/public.swarm_283.mjs";
import { public_swarm_284Row } from "./zod/public.swarm_284.mjs";
import { public_swarm_285Row } from "./zod/public.swarm_285.mjs";
import { public_swarm_286Row } from "./zod/public.swarm_286.mjs";
import { public_swarm_287Row } from "./zod/public.swarm_287.mjs";
import { public_swarm_288Row } from "./zod/public.swarm_288.mjs";
import { public_swarm_289Row } from "./zod/public.swarm_289.mjs";
import { public_swarm_29Row } from "./zod/public.swarm_29.mjs";
import { public_swarm_290Row } from "./zod/public.swarm_290.mjs";
import { public_swarm_291Row } from "./zod/public.swarm_291.mjs";
import { public_swarm_292Row } from "./zod/public.swarm_292.mjs";
import { public_swarm_293Row } from "./zod/public.swarm_293.mjs";
import { public_swarm_294Row } from "./zod/public.swarm_294.mjs";
import { public_swarm_295Row } from "./zod/public.swarm_295.mjs";
import { public_swarm_296Row } from "./zod/public.swarm_296.mjs";
import { public_swarm_297Row } from "./zod/public.swarm_297.mjs";
import { public_swarm_298Row } from "./zod/public.swarm_298.mjs";
import { public_swarm_299Row } from "./zod/public.swarm_299.mjs";
import { public_swarm_3Row } from "./zod/public.swarm_3.mjs";
import { public_swarm_30Row } from "./zod/public.swarm_30.mjs";
import { public_swarm_300Row } from "./zod/public.swarm_300.mjs";
import { public_swarm_31Row } from "./zod/public.swarm_31.mjs";
import { public_swarm_32Row } from "./zod/public.swarm_32.mjs";
import { public_swarm_33Row } from "./zod/public.swarm_33.mjs";
import { public_swarm_34Row } from "./zod/public.swarm_34.mjs";
import { public_swarm_35Row } from "./zod/public.swarm_35.mjs";
import { public_swarm_36Row } from "./zod/public.swarm_36.mjs";
import { public_swarm_37Row } from "./zod/public.swarm_37.mjs";
import { public_swarm_38Row } from "./zod/public.swarm_38.mjs";
import { public_swarm_39Row } from "./zod/public.swarm_39.mjs";
import { public_swarm_4Row } from "./zod/public.swarm_4.mjs";
import { public_swarm_40Row } from "./zod/public.swarm_40.mjs";
import { public_swarm_41Row } from "./zod/public.swarm_41.mjs";
import { public_swarm_42Row } from "./zod/public.swarm_42.mjs";
import { public_swarm_43Row } from "./zod/public.swarm_43.mjs";
import { public_swarm_44Row } from "./zod/public.swarm_44.mjs";
import { public_swarm_45Row } from "./zod/public.swarm_45.mjs";
import { public_swarm_46Row } from "./zod/public.swarm_46.mjs";
import { public_swarm_47Row } from "./zod/public.swarm_47.mjs";
import { public_swarm_48Row } from "./zod/public.swarm_48.mjs";
import { public_swarm_49Row } from "./zod/public.swarm_49.mjs";
import { public_swarm_5Row } from "./zod/public.swarm_5.mjs";
import { public_swarm_50Row } from "./zod/public.swarm_50.mjs";
import { public_swarm_51Row } from "./zod/public.swarm_51.mjs";
import { public_swarm_52Row } from "./zod/public.swarm_52.mjs";
import { public_swarm_53Row } from "./zod/public.swarm_53.mjs";
import { public_swarm_54Row } from "./zod/public.swarm_54.mjs";
import { public_swarm_55Row } from "./zod/public.swarm_55.mjs";
import { public_swarm_56Row } from "./zod/public.swarm_56.mjs";
import { public_swarm_57Row } from "./zod/public.swarm_57.mjs";
import { public_swarm_58Row } from "./zod/public.swarm_58.mjs";
import { public_swarm_59Row } from "./zod/public.swarm_59.mjs";
import { public_swarm_6Row } from "./zod/public.swarm_6.mjs";
import { public_swarm_60Row } from "./zod/public.swarm_60.mjs";
import { public_swarm_61Row } from "./zod/public.swarm_61.mjs";
import { public_swarm_62Row } from "./zod/public.swarm_62.mjs";
import { public_swarm_63Row } from "./zod/public.swarm_63.mjs";
import { public_swarm_64Row } from "./zod/public.swarm_64.mjs";
import { public_swarm_65Row } from "./zod/public.swarm_65.mjs";
import { public_swarm_66Row } from "./zod/public.swarm_66.mjs";
import { public_swarm_67Row } from "./zod/public.swarm_67.mjs";
import { public_swarm_68Row } from "./zod/public.swarm_68.mjs";
import { public_swarm_69Row } from "./zod/public.swarm_69.mjs";
import { public_swarm_7Row } from "./zod/public.swarm_7.mjs";
import { public_swarm_70Row } from "./zod/public.swarm_70.mjs";
import { public_swarm_71Row } from "./zod/public.swarm_71.mjs";
import { public_swarm_72Row } from "./zod/public.swarm_72.mjs";
import { public_swarm_73Row } from "./zod/public.swarm_73.mjs";
import { public_swarm_74Row } from "./zod/public.swarm_74.mjs";
import { public_swarm_75Row } from "./zod/public.swarm_75.mjs";
import { public_swarm_76Row } from "./zod/public.swarm_76.mjs";
import { public_swarm_77Row } from "./zod/public.swarm_77.mjs";
import { public_swarm_78Row } from "./zod/public.swarm_78.mjs";
import { public_swarm_79Row } from "./zod/public.swarm_79.mjs";
import { public_swarm_8Row } from "./zod/public.swarm_8.mjs";
import { public_swarm_80Row } from "./zod/public.swarm_80.mjs";
import { public_swarm_81Row } from "./zod/public.swarm_81.mjs";
import { public_swarm_82Row } from "./zod/public.swarm_82.mjs";
import { public_swarm_83Row } from "./zod/public.swarm_83.mjs";
import { public_swarm_84Row } from "./zod/public.swarm_84.mjs";
import { public_swarm_85Row } from "./zod/public.swarm_85.mjs";
import { public_swarm_86Row } from "./zod/public.swarm_86.mjs";
import { public_swarm_87Row } from "./zod/public.swarm_87.mjs";
import { public_swarm_88Row } from "./zod/public.swarm_88.mjs";
import { public_swarm_89Row } from "./zod/public.swarm_89.mjs";
import { public_swarm_9Row } from "./zod/public.swarm_9.mjs";
import { public_swarm_90Row } from "./zod/public.swarm_90.mjs";
import { public_swarm_91Row } from "./zod/public.swarm_91.mjs";
import { public_swarm_92Row } from "./zod/public.swarm_92.mjs";
import { public_swarm_93Row } from "./zod/public.swarm_93.mjs";
import { public_swarm_94Row } from "./zod/public.swarm_94.mjs";
import { public_swarm_95Row } from "./zod/public.swarm_95.mjs";
import { public_swarm_96Row } from "./zod/public.swarm_96.mjs";
import { public_swarm_97Row } from "./zod/public.swarm_97.mjs";
import { public_swarm_98Row } from "./zod/public.swarm_98.mjs";
import { public_swarm_99Row } from "./zod/public.swarm_99.mjs";
import { public_teleRow } from "./zod/public.tele.mjs";
import { public_usersRow } from "./zod/public.users.mjs";
import { public_users__drop_table_users__Row } from "./zod/public.users; drop table users--.mjs";
import { public_with_droppedRow } from "./zod/public.with_dropped.mjs";

export function createMcpServer({ sql, name = "supawatch-db", version = "0.0.0" }) {
  const server = new McpServer({ name, version });
  server.tool(
    "public_USERS_list",
    "List rows from public.USERS",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."USERS" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_USERSRow.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_USERS_get",
    "Fetch one row from public.USERS by id",
    public_USERSRow.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."USERS" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_USERSRow.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_Users_list",
    "List rows from public.Users",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."Users" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_UsersRow.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_Users_get",
    "Fetch one row from public.Users by id",
    public_UsersRow.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."Users" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_UsersRow.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_audited_list",
    "List rows from public.audited",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."audited" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_auditedRow.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_audited_get",
    "Fetch one row from public.audited by id",
    public_auditedRow.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."audited" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_auditedRow.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_chain_1_list",
    "List rows from public.chain_1",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."chain_1" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_chain_1Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_chain_1_get",
    "Fetch one row from public.chain_1 by id",
    public_chain_1Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."chain_1" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_chain_1Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_chain_10_list",
    "List rows from public.chain_10",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."chain_10" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_chain_10Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_chain_10_get",
    "Fetch one row from public.chain_10 by id",
    public_chain_10Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."chain_10" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_chain_10Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_chain_11_list",
    "List rows from public.chain_11",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."chain_11" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_chain_11Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_chain_11_get",
    "Fetch one row from public.chain_11 by id",
    public_chain_11Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."chain_11" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_chain_11Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_chain_12_list",
    "List rows from public.chain_12",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."chain_12" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_chain_12Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_chain_12_get",
    "Fetch one row from public.chain_12 by id",
    public_chain_12Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."chain_12" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_chain_12Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_chain_13_list",
    "List rows from public.chain_13",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."chain_13" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_chain_13Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_chain_13_get",
    "Fetch one row from public.chain_13 by id",
    public_chain_13Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."chain_13" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_chain_13Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_chain_14_list",
    "List rows from public.chain_14",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."chain_14" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_chain_14Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_chain_14_get",
    "Fetch one row from public.chain_14 by id",
    public_chain_14Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."chain_14" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_chain_14Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_chain_15_list",
    "List rows from public.chain_15",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."chain_15" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_chain_15Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_chain_15_get",
    "Fetch one row from public.chain_15 by id",
    public_chain_15Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."chain_15" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_chain_15Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_chain_16_list",
    "List rows from public.chain_16",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."chain_16" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_chain_16Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_chain_16_get",
    "Fetch one row from public.chain_16 by id",
    public_chain_16Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."chain_16" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_chain_16Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_chain_17_list",
    "List rows from public.chain_17",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."chain_17" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_chain_17Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_chain_17_get",
    "Fetch one row from public.chain_17 by id",
    public_chain_17Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."chain_17" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_chain_17Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_chain_18_list",
    "List rows from public.chain_18",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."chain_18" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_chain_18Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_chain_18_get",
    "Fetch one row from public.chain_18 by id",
    public_chain_18Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."chain_18" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_chain_18Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_chain_19_list",
    "List rows from public.chain_19",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."chain_19" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_chain_19Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_chain_19_get",
    "Fetch one row from public.chain_19 by id",
    public_chain_19Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."chain_19" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_chain_19Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_chain_2_list",
    "List rows from public.chain_2",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."chain_2" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_chain_2Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_chain_2_get",
    "Fetch one row from public.chain_2 by id",
    public_chain_2Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."chain_2" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_chain_2Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_chain_20_list",
    "List rows from public.chain_20",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."chain_20" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_chain_20Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_chain_20_get",
    "Fetch one row from public.chain_20 by id",
    public_chain_20Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."chain_20" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_chain_20Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_chain_21_list",
    "List rows from public.chain_21",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."chain_21" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_chain_21Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_chain_21_get",
    "Fetch one row from public.chain_21 by id",
    public_chain_21Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."chain_21" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_chain_21Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_chain_22_list",
    "List rows from public.chain_22",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."chain_22" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_chain_22Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_chain_22_get",
    "Fetch one row from public.chain_22 by id",
    public_chain_22Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."chain_22" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_chain_22Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_chain_23_list",
    "List rows from public.chain_23",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."chain_23" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_chain_23Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_chain_23_get",
    "Fetch one row from public.chain_23 by id",
    public_chain_23Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."chain_23" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_chain_23Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_chain_24_list",
    "List rows from public.chain_24",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."chain_24" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_chain_24Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_chain_24_get",
    "Fetch one row from public.chain_24 by id",
    public_chain_24Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."chain_24" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_chain_24Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_chain_25_list",
    "List rows from public.chain_25",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."chain_25" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_chain_25Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_chain_25_get",
    "Fetch one row from public.chain_25 by id",
    public_chain_25Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."chain_25" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_chain_25Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_chain_26_list",
    "List rows from public.chain_26",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."chain_26" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_chain_26Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_chain_26_get",
    "Fetch one row from public.chain_26 by id",
    public_chain_26Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."chain_26" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_chain_26Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_chain_27_list",
    "List rows from public.chain_27",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."chain_27" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_chain_27Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_chain_27_get",
    "Fetch one row from public.chain_27 by id",
    public_chain_27Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."chain_27" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_chain_27Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_chain_28_list",
    "List rows from public.chain_28",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."chain_28" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_chain_28Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_chain_28_get",
    "Fetch one row from public.chain_28 by id",
    public_chain_28Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."chain_28" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_chain_28Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_chain_29_list",
    "List rows from public.chain_29",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."chain_29" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_chain_29Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_chain_29_get",
    "Fetch one row from public.chain_29 by id",
    public_chain_29Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."chain_29" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_chain_29Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_chain_3_list",
    "List rows from public.chain_3",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."chain_3" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_chain_3Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_chain_3_get",
    "Fetch one row from public.chain_3 by id",
    public_chain_3Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."chain_3" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_chain_3Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_chain_30_list",
    "List rows from public.chain_30",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."chain_30" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_chain_30Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_chain_30_get",
    "Fetch one row from public.chain_30 by id",
    public_chain_30Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."chain_30" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_chain_30Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_chain_4_list",
    "List rows from public.chain_4",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."chain_4" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_chain_4Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_chain_4_get",
    "Fetch one row from public.chain_4 by id",
    public_chain_4Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."chain_4" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_chain_4Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_chain_5_list",
    "List rows from public.chain_5",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."chain_5" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_chain_5Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_chain_5_get",
    "Fetch one row from public.chain_5 by id",
    public_chain_5Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."chain_5" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_chain_5Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_chain_6_list",
    "List rows from public.chain_6",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."chain_6" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_chain_6Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_chain_6_get",
    "Fetch one row from public.chain_6 by id",
    public_chain_6Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."chain_6" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_chain_6Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_chain_7_list",
    "List rows from public.chain_7",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."chain_7" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_chain_7Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_chain_7_get",
    "Fetch one row from public.chain_7 by id",
    public_chain_7Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."chain_7" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_chain_7Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_chain_8_list",
    "List rows from public.chain_8",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."chain_8" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_chain_8Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_chain_8_get",
    "Fetch one row from public.chain_8 by id",
    public_chain_8Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."chain_8" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_chain_8Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_chain_9_list",
    "List rows from public.chain_9",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."chain_9" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_chain_9Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_chain_9_get",
    "Fetch one row from public.chain_9 by id",
    public_chain_9Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."chain_9" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_chain_9Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_clocked_list",
    "List rows from public.clocked",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."clocked" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_clockedRow.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_clocked_get",
    "Fetch one row from public.clocked by span",
    public_clockedRow.pick({ "span": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."clocked" where "span" = $1 limit 1', [input["span"]]);
      const row = rows.length > 0 ? public_clockedRow.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_colossus_list",
    "List rows from public.colossus",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."colossus" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_colossusRow.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_colossus_get",
    "Fetch one row from public.colossus by id",
    public_colossusRow.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."colossus" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_colossusRow.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_default_list",
    "List rows from public.default",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."default" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_defaultRow.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_default_get",
    "Fetch one row from public.default by id",
    public_defaultRow.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."default" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_defaultRow.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_extremes_list",
    "List rows from public.extremes",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."extremes" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_extremesRow.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_extremes_get",
    "Fetch one row from public.extremes by id",
    public_extremesRow.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."extremes" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_extremesRow.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_gen_children_list",
    "List rows from public.gen_children",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."gen_children" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_gen_childrenRow.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_gen_children_get",
    "Fetch one row from public.gen_children by id",
    public_gen_childrenRow.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."gen_children" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_gen_childrenRow.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_gen_parents_list",
    "List rows from public.gen_parents",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."gen_parents" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_gen_parentsRow.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_gen_parents_get",
    "Fetch one row from public.gen_parents by id",
    public_gen_parentsRow.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."gen_parents" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_gen_parentsRow.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_kilo_list",
    "List rows from public.kilo",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."kilo" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_kiloRow.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_kilo_get",
    "Fetch one row from public.kilo by id",
    public_kiloRow.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."kilo" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_kiloRow.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "aux_leaves_list",
    "List rows from aux.leaves",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "aux"."leaves" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => aux_leavesRow.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "aux_leaves_get",
    "Fetch one row from aux.leaves by id",
    aux_leavesRow.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "aux"."leaves" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? aux_leavesRow.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_new_list",
    "List rows from public.new",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."new" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_newRow.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_new_get",
    "Fetch one row from public.new by id",
    public_newRow.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."new" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_newRow.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "___panic_list",
    "List rows from 😱.panic",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "😱"."panic" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => ___panicRow.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "___panic_get",
    "Fetch one row from 😱.panic by id",
    ___panicRow.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "😱"."panic" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? ___panicRow.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_reserved_cols_list",
    "List rows from public.reserved_cols",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."reserved_cols" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_reserved_colsRow.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_reserved_cols_get",
    "Fetch one row from public.reserved_cols by id",
    public_reserved_colsRow.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."reserved_cols" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_reserved_colsRow.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_roots_list",
    "List rows from public.roots",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."roots" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_rootsRow.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_roots_get",
    "Fetch one row from public.roots by id",
    public_rootsRow.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."roots" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_rootsRow.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_share_a_list",
    "List rows from public.share_a",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."share_a" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_share_aRow.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_share_a_get",
    "Fetch one row from public.share_a by id",
    public_share_aRow.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."share_a" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_share_aRow.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_share_b_list",
    "List rows from public.share_b",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."share_b" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_share_bRow.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_share_b_get",
    "Fetch one row from public.share_b by id",
    public_share_bRow.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."share_b" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_share_bRow.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_stamped_list",
    "List rows from public.stamped",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."stamped" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_stampedRow.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_stamped_get",
    "Fetch one row from public.stamped by id",
    public_stampedRow.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."stamped" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_stampedRow.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_1_list",
    "List rows from public.swarm_1",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_1" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_1Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_1_get",
    "Fetch one row from public.swarm_1 by id",
    public_swarm_1Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_1" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_1Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_10_list",
    "List rows from public.swarm_10",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_10" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_10Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_10_get",
    "Fetch one row from public.swarm_10 by id",
    public_swarm_10Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_10" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_10Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_100_list",
    "List rows from public.swarm_100",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_100" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_100Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_100_get",
    "Fetch one row from public.swarm_100 by id",
    public_swarm_100Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_100" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_100Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_101_list",
    "List rows from public.swarm_101",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_101" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_101Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_101_get",
    "Fetch one row from public.swarm_101 by id",
    public_swarm_101Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_101" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_101Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_102_list",
    "List rows from public.swarm_102",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_102" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_102Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_102_get",
    "Fetch one row from public.swarm_102 by id",
    public_swarm_102Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_102" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_102Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_103_list",
    "List rows from public.swarm_103",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_103" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_103Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_103_get",
    "Fetch one row from public.swarm_103 by id",
    public_swarm_103Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_103" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_103Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_104_list",
    "List rows from public.swarm_104",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_104" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_104Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_104_get",
    "Fetch one row from public.swarm_104 by id",
    public_swarm_104Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_104" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_104Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_105_list",
    "List rows from public.swarm_105",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_105" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_105Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_105_get",
    "Fetch one row from public.swarm_105 by id",
    public_swarm_105Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_105" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_105Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_106_list",
    "List rows from public.swarm_106",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_106" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_106Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_106_get",
    "Fetch one row from public.swarm_106 by id",
    public_swarm_106Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_106" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_106Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_107_list",
    "List rows from public.swarm_107",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_107" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_107Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_107_get",
    "Fetch one row from public.swarm_107 by id",
    public_swarm_107Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_107" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_107Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_108_list",
    "List rows from public.swarm_108",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_108" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_108Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_108_get",
    "Fetch one row from public.swarm_108 by id",
    public_swarm_108Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_108" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_108Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_109_list",
    "List rows from public.swarm_109",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_109" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_109Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_109_get",
    "Fetch one row from public.swarm_109 by id",
    public_swarm_109Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_109" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_109Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_11_list",
    "List rows from public.swarm_11",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_11" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_11Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_11_get",
    "Fetch one row from public.swarm_11 by id",
    public_swarm_11Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_11" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_11Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_110_list",
    "List rows from public.swarm_110",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_110" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_110Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_110_get",
    "Fetch one row from public.swarm_110 by id",
    public_swarm_110Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_110" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_110Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_111_list",
    "List rows from public.swarm_111",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_111" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_111Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_111_get",
    "Fetch one row from public.swarm_111 by id",
    public_swarm_111Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_111" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_111Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_112_list",
    "List rows from public.swarm_112",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_112" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_112Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_112_get",
    "Fetch one row from public.swarm_112 by id",
    public_swarm_112Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_112" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_112Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_113_list",
    "List rows from public.swarm_113",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_113" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_113Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_113_get",
    "Fetch one row from public.swarm_113 by id",
    public_swarm_113Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_113" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_113Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_114_list",
    "List rows from public.swarm_114",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_114" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_114Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_114_get",
    "Fetch one row from public.swarm_114 by id",
    public_swarm_114Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_114" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_114Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_115_list",
    "List rows from public.swarm_115",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_115" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_115Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_115_get",
    "Fetch one row from public.swarm_115 by id",
    public_swarm_115Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_115" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_115Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_116_list",
    "List rows from public.swarm_116",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_116" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_116Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_116_get",
    "Fetch one row from public.swarm_116 by id",
    public_swarm_116Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_116" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_116Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_117_list",
    "List rows from public.swarm_117",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_117" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_117Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_117_get",
    "Fetch one row from public.swarm_117 by id",
    public_swarm_117Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_117" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_117Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_118_list",
    "List rows from public.swarm_118",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_118" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_118Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_118_get",
    "Fetch one row from public.swarm_118 by id",
    public_swarm_118Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_118" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_118Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_119_list",
    "List rows from public.swarm_119",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_119" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_119Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_119_get",
    "Fetch one row from public.swarm_119 by id",
    public_swarm_119Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_119" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_119Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_12_list",
    "List rows from public.swarm_12",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_12" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_12Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_12_get",
    "Fetch one row from public.swarm_12 by id",
    public_swarm_12Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_12" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_12Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_120_list",
    "List rows from public.swarm_120",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_120" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_120Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_120_get",
    "Fetch one row from public.swarm_120 by id",
    public_swarm_120Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_120" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_120Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_121_list",
    "List rows from public.swarm_121",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_121" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_121Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_121_get",
    "Fetch one row from public.swarm_121 by id",
    public_swarm_121Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_121" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_121Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_122_list",
    "List rows from public.swarm_122",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_122" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_122Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_122_get",
    "Fetch one row from public.swarm_122 by id",
    public_swarm_122Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_122" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_122Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_123_list",
    "List rows from public.swarm_123",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_123" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_123Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_123_get",
    "Fetch one row from public.swarm_123 by id",
    public_swarm_123Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_123" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_123Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_124_list",
    "List rows from public.swarm_124",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_124" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_124Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_124_get",
    "Fetch one row from public.swarm_124 by id",
    public_swarm_124Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_124" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_124Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_125_list",
    "List rows from public.swarm_125",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_125" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_125Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_125_get",
    "Fetch one row from public.swarm_125 by id",
    public_swarm_125Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_125" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_125Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_126_list",
    "List rows from public.swarm_126",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_126" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_126Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_126_get",
    "Fetch one row from public.swarm_126 by id",
    public_swarm_126Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_126" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_126Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_127_list",
    "List rows from public.swarm_127",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_127" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_127Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_127_get",
    "Fetch one row from public.swarm_127 by id",
    public_swarm_127Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_127" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_127Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_128_list",
    "List rows from public.swarm_128",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_128" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_128Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_128_get",
    "Fetch one row from public.swarm_128 by id",
    public_swarm_128Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_128" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_128Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_129_list",
    "List rows from public.swarm_129",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_129" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_129Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_129_get",
    "Fetch one row from public.swarm_129 by id",
    public_swarm_129Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_129" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_129Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_13_list",
    "List rows from public.swarm_13",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_13" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_13Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_13_get",
    "Fetch one row from public.swarm_13 by id",
    public_swarm_13Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_13" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_13Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_130_list",
    "List rows from public.swarm_130",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_130" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_130Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_130_get",
    "Fetch one row from public.swarm_130 by id",
    public_swarm_130Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_130" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_130Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_131_list",
    "List rows from public.swarm_131",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_131" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_131Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_131_get",
    "Fetch one row from public.swarm_131 by id",
    public_swarm_131Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_131" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_131Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_132_list",
    "List rows from public.swarm_132",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_132" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_132Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_132_get",
    "Fetch one row from public.swarm_132 by id",
    public_swarm_132Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_132" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_132Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_133_list",
    "List rows from public.swarm_133",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_133" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_133Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_133_get",
    "Fetch one row from public.swarm_133 by id",
    public_swarm_133Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_133" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_133Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_134_list",
    "List rows from public.swarm_134",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_134" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_134Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_134_get",
    "Fetch one row from public.swarm_134 by id",
    public_swarm_134Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_134" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_134Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_135_list",
    "List rows from public.swarm_135",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_135" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_135Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_135_get",
    "Fetch one row from public.swarm_135 by id",
    public_swarm_135Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_135" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_135Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_136_list",
    "List rows from public.swarm_136",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_136" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_136Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_136_get",
    "Fetch one row from public.swarm_136 by id",
    public_swarm_136Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_136" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_136Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_137_list",
    "List rows from public.swarm_137",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_137" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_137Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_137_get",
    "Fetch one row from public.swarm_137 by id",
    public_swarm_137Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_137" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_137Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_138_list",
    "List rows from public.swarm_138",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_138" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_138Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_138_get",
    "Fetch one row from public.swarm_138 by id",
    public_swarm_138Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_138" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_138Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_139_list",
    "List rows from public.swarm_139",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_139" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_139Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_139_get",
    "Fetch one row from public.swarm_139 by id",
    public_swarm_139Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_139" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_139Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_14_list",
    "List rows from public.swarm_14",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_14" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_14Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_14_get",
    "Fetch one row from public.swarm_14 by id",
    public_swarm_14Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_14" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_14Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_140_list",
    "List rows from public.swarm_140",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_140" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_140Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_140_get",
    "Fetch one row from public.swarm_140 by id",
    public_swarm_140Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_140" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_140Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_141_list",
    "List rows from public.swarm_141",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_141" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_141Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_141_get",
    "Fetch one row from public.swarm_141 by id",
    public_swarm_141Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_141" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_141Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_142_list",
    "List rows from public.swarm_142",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_142" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_142Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_142_get",
    "Fetch one row from public.swarm_142 by id",
    public_swarm_142Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_142" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_142Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_143_list",
    "List rows from public.swarm_143",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_143" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_143Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_143_get",
    "Fetch one row from public.swarm_143 by id",
    public_swarm_143Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_143" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_143Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_144_list",
    "List rows from public.swarm_144",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_144" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_144Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_144_get",
    "Fetch one row from public.swarm_144 by id",
    public_swarm_144Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_144" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_144Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_145_list",
    "List rows from public.swarm_145",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_145" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_145Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_145_get",
    "Fetch one row from public.swarm_145 by id",
    public_swarm_145Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_145" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_145Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_146_list",
    "List rows from public.swarm_146",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_146" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_146Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_146_get",
    "Fetch one row from public.swarm_146 by id",
    public_swarm_146Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_146" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_146Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_147_list",
    "List rows from public.swarm_147",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_147" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_147Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_147_get",
    "Fetch one row from public.swarm_147 by id",
    public_swarm_147Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_147" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_147Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_148_list",
    "List rows from public.swarm_148",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_148" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_148Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_148_get",
    "Fetch one row from public.swarm_148 by id",
    public_swarm_148Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_148" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_148Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_149_list",
    "List rows from public.swarm_149",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_149" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_149Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_149_get",
    "Fetch one row from public.swarm_149 by id",
    public_swarm_149Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_149" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_149Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_15_list",
    "List rows from public.swarm_15",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_15" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_15Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_15_get",
    "Fetch one row from public.swarm_15 by id",
    public_swarm_15Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_15" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_15Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_150_list",
    "List rows from public.swarm_150",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_150" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_150Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_150_get",
    "Fetch one row from public.swarm_150 by id",
    public_swarm_150Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_150" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_150Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_151_list",
    "List rows from public.swarm_151",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_151" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_151Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_151_get",
    "Fetch one row from public.swarm_151 by id",
    public_swarm_151Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_151" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_151Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_152_list",
    "List rows from public.swarm_152",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_152" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_152Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_152_get",
    "Fetch one row from public.swarm_152 by id",
    public_swarm_152Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_152" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_152Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_153_list",
    "List rows from public.swarm_153",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_153" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_153Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_153_get",
    "Fetch one row from public.swarm_153 by id",
    public_swarm_153Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_153" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_153Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_154_list",
    "List rows from public.swarm_154",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_154" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_154Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_154_get",
    "Fetch one row from public.swarm_154 by id",
    public_swarm_154Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_154" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_154Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_155_list",
    "List rows from public.swarm_155",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_155" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_155Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_155_get",
    "Fetch one row from public.swarm_155 by id",
    public_swarm_155Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_155" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_155Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_156_list",
    "List rows from public.swarm_156",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_156" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_156Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_156_get",
    "Fetch one row from public.swarm_156 by id",
    public_swarm_156Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_156" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_156Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_157_list",
    "List rows from public.swarm_157",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_157" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_157Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_157_get",
    "Fetch one row from public.swarm_157 by id",
    public_swarm_157Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_157" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_157Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_158_list",
    "List rows from public.swarm_158",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_158" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_158Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_158_get",
    "Fetch one row from public.swarm_158 by id",
    public_swarm_158Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_158" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_158Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_159_list",
    "List rows from public.swarm_159",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_159" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_159Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_159_get",
    "Fetch one row from public.swarm_159 by id",
    public_swarm_159Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_159" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_159Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_16_list",
    "List rows from public.swarm_16",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_16" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_16Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_16_get",
    "Fetch one row from public.swarm_16 by id",
    public_swarm_16Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_16" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_16Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_160_list",
    "List rows from public.swarm_160",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_160" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_160Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_160_get",
    "Fetch one row from public.swarm_160 by id",
    public_swarm_160Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_160" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_160Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_161_list",
    "List rows from public.swarm_161",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_161" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_161Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_161_get",
    "Fetch one row from public.swarm_161 by id",
    public_swarm_161Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_161" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_161Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_162_list",
    "List rows from public.swarm_162",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_162" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_162Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_162_get",
    "Fetch one row from public.swarm_162 by id",
    public_swarm_162Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_162" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_162Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_163_list",
    "List rows from public.swarm_163",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_163" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_163Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_163_get",
    "Fetch one row from public.swarm_163 by id",
    public_swarm_163Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_163" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_163Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_164_list",
    "List rows from public.swarm_164",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_164" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_164Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_164_get",
    "Fetch one row from public.swarm_164 by id",
    public_swarm_164Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_164" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_164Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_165_list",
    "List rows from public.swarm_165",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_165" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_165Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_165_get",
    "Fetch one row from public.swarm_165 by id",
    public_swarm_165Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_165" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_165Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_166_list",
    "List rows from public.swarm_166",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_166" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_166Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_166_get",
    "Fetch one row from public.swarm_166 by id",
    public_swarm_166Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_166" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_166Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_167_list",
    "List rows from public.swarm_167",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_167" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_167Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_167_get",
    "Fetch one row from public.swarm_167 by id",
    public_swarm_167Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_167" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_167Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_168_list",
    "List rows from public.swarm_168",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_168" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_168Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_168_get",
    "Fetch one row from public.swarm_168 by id",
    public_swarm_168Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_168" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_168Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_169_list",
    "List rows from public.swarm_169",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_169" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_169Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_169_get",
    "Fetch one row from public.swarm_169 by id",
    public_swarm_169Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_169" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_169Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_17_list",
    "List rows from public.swarm_17",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_17" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_17Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_17_get",
    "Fetch one row from public.swarm_17 by id",
    public_swarm_17Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_17" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_17Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_170_list",
    "List rows from public.swarm_170",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_170" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_170Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_170_get",
    "Fetch one row from public.swarm_170 by id",
    public_swarm_170Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_170" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_170Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_171_list",
    "List rows from public.swarm_171",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_171" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_171Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_171_get",
    "Fetch one row from public.swarm_171 by id",
    public_swarm_171Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_171" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_171Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_172_list",
    "List rows from public.swarm_172",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_172" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_172Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_172_get",
    "Fetch one row from public.swarm_172 by id",
    public_swarm_172Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_172" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_172Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_173_list",
    "List rows from public.swarm_173",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_173" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_173Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_173_get",
    "Fetch one row from public.swarm_173 by id",
    public_swarm_173Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_173" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_173Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_174_list",
    "List rows from public.swarm_174",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_174" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_174Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_174_get",
    "Fetch one row from public.swarm_174 by id",
    public_swarm_174Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_174" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_174Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_175_list",
    "List rows from public.swarm_175",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_175" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_175Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_175_get",
    "Fetch one row from public.swarm_175 by id",
    public_swarm_175Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_175" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_175Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_176_list",
    "List rows from public.swarm_176",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_176" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_176Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_176_get",
    "Fetch one row from public.swarm_176 by id",
    public_swarm_176Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_176" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_176Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_177_list",
    "List rows from public.swarm_177",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_177" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_177Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_177_get",
    "Fetch one row from public.swarm_177 by id",
    public_swarm_177Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_177" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_177Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_178_list",
    "List rows from public.swarm_178",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_178" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_178Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_178_get",
    "Fetch one row from public.swarm_178 by id",
    public_swarm_178Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_178" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_178Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_179_list",
    "List rows from public.swarm_179",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_179" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_179Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_179_get",
    "Fetch one row from public.swarm_179 by id",
    public_swarm_179Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_179" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_179Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_18_list",
    "List rows from public.swarm_18",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_18" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_18Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_18_get",
    "Fetch one row from public.swarm_18 by id",
    public_swarm_18Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_18" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_18Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_180_list",
    "List rows from public.swarm_180",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_180" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_180Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_180_get",
    "Fetch one row from public.swarm_180 by id",
    public_swarm_180Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_180" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_180Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_181_list",
    "List rows from public.swarm_181",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_181" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_181Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_181_get",
    "Fetch one row from public.swarm_181 by id",
    public_swarm_181Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_181" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_181Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_182_list",
    "List rows from public.swarm_182",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_182" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_182Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_182_get",
    "Fetch one row from public.swarm_182 by id",
    public_swarm_182Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_182" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_182Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_183_list",
    "List rows from public.swarm_183",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_183" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_183Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_183_get",
    "Fetch one row from public.swarm_183 by id",
    public_swarm_183Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_183" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_183Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_184_list",
    "List rows from public.swarm_184",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_184" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_184Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_184_get",
    "Fetch one row from public.swarm_184 by id",
    public_swarm_184Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_184" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_184Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_185_list",
    "List rows from public.swarm_185",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_185" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_185Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_185_get",
    "Fetch one row from public.swarm_185 by id",
    public_swarm_185Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_185" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_185Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_186_list",
    "List rows from public.swarm_186",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_186" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_186Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_186_get",
    "Fetch one row from public.swarm_186 by id",
    public_swarm_186Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_186" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_186Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_187_list",
    "List rows from public.swarm_187",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_187" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_187Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_187_get",
    "Fetch one row from public.swarm_187 by id",
    public_swarm_187Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_187" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_187Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_188_list",
    "List rows from public.swarm_188",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_188" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_188Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_188_get",
    "Fetch one row from public.swarm_188 by id",
    public_swarm_188Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_188" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_188Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_189_list",
    "List rows from public.swarm_189",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_189" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_189Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_189_get",
    "Fetch one row from public.swarm_189 by id",
    public_swarm_189Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_189" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_189Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_19_list",
    "List rows from public.swarm_19",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_19" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_19Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_19_get",
    "Fetch one row from public.swarm_19 by id",
    public_swarm_19Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_19" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_19Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_190_list",
    "List rows from public.swarm_190",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_190" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_190Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_190_get",
    "Fetch one row from public.swarm_190 by id",
    public_swarm_190Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_190" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_190Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_191_list",
    "List rows from public.swarm_191",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_191" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_191Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_191_get",
    "Fetch one row from public.swarm_191 by id",
    public_swarm_191Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_191" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_191Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_192_list",
    "List rows from public.swarm_192",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_192" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_192Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_192_get",
    "Fetch one row from public.swarm_192 by id",
    public_swarm_192Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_192" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_192Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_193_list",
    "List rows from public.swarm_193",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_193" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_193Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_193_get",
    "Fetch one row from public.swarm_193 by id",
    public_swarm_193Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_193" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_193Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_194_list",
    "List rows from public.swarm_194",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_194" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_194Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_194_get",
    "Fetch one row from public.swarm_194 by id",
    public_swarm_194Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_194" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_194Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_195_list",
    "List rows from public.swarm_195",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_195" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_195Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_195_get",
    "Fetch one row from public.swarm_195 by id",
    public_swarm_195Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_195" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_195Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_196_list",
    "List rows from public.swarm_196",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_196" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_196Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_196_get",
    "Fetch one row from public.swarm_196 by id",
    public_swarm_196Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_196" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_196Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_197_list",
    "List rows from public.swarm_197",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_197" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_197Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_197_get",
    "Fetch one row from public.swarm_197 by id",
    public_swarm_197Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_197" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_197Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_198_list",
    "List rows from public.swarm_198",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_198" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_198Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_198_get",
    "Fetch one row from public.swarm_198 by id",
    public_swarm_198Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_198" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_198Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_199_list",
    "List rows from public.swarm_199",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_199" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_199Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_199_get",
    "Fetch one row from public.swarm_199 by id",
    public_swarm_199Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_199" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_199Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_2_list",
    "List rows from public.swarm_2",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_2" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_2Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_2_get",
    "Fetch one row from public.swarm_2 by id",
    public_swarm_2Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_2" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_2Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_20_list",
    "List rows from public.swarm_20",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_20" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_20Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_20_get",
    "Fetch one row from public.swarm_20 by id",
    public_swarm_20Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_20" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_20Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_200_list",
    "List rows from public.swarm_200",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_200" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_200Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_200_get",
    "Fetch one row from public.swarm_200 by id",
    public_swarm_200Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_200" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_200Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_201_list",
    "List rows from public.swarm_201",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_201" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_201Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_201_get",
    "Fetch one row from public.swarm_201 by id",
    public_swarm_201Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_201" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_201Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_202_list",
    "List rows from public.swarm_202",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_202" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_202Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_202_get",
    "Fetch one row from public.swarm_202 by id",
    public_swarm_202Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_202" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_202Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_203_list",
    "List rows from public.swarm_203",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_203" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_203Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_203_get",
    "Fetch one row from public.swarm_203 by id",
    public_swarm_203Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_203" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_203Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_204_list",
    "List rows from public.swarm_204",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_204" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_204Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_204_get",
    "Fetch one row from public.swarm_204 by id",
    public_swarm_204Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_204" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_204Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_205_list",
    "List rows from public.swarm_205",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_205" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_205Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_205_get",
    "Fetch one row from public.swarm_205 by id",
    public_swarm_205Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_205" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_205Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_206_list",
    "List rows from public.swarm_206",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_206" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_206Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_206_get",
    "Fetch one row from public.swarm_206 by id",
    public_swarm_206Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_206" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_206Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_207_list",
    "List rows from public.swarm_207",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_207" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_207Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_207_get",
    "Fetch one row from public.swarm_207 by id",
    public_swarm_207Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_207" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_207Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_208_list",
    "List rows from public.swarm_208",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_208" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_208Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_208_get",
    "Fetch one row from public.swarm_208 by id",
    public_swarm_208Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_208" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_208Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_209_list",
    "List rows from public.swarm_209",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_209" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_209Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_209_get",
    "Fetch one row from public.swarm_209 by id",
    public_swarm_209Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_209" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_209Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_21_list",
    "List rows from public.swarm_21",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_21" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_21Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_21_get",
    "Fetch one row from public.swarm_21 by id",
    public_swarm_21Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_21" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_21Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_210_list",
    "List rows from public.swarm_210",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_210" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_210Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_210_get",
    "Fetch one row from public.swarm_210 by id",
    public_swarm_210Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_210" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_210Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_211_list",
    "List rows from public.swarm_211",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_211" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_211Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_211_get",
    "Fetch one row from public.swarm_211 by id",
    public_swarm_211Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_211" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_211Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_212_list",
    "List rows from public.swarm_212",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_212" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_212Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_212_get",
    "Fetch one row from public.swarm_212 by id",
    public_swarm_212Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_212" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_212Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_213_list",
    "List rows from public.swarm_213",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_213" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_213Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_213_get",
    "Fetch one row from public.swarm_213 by id",
    public_swarm_213Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_213" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_213Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_214_list",
    "List rows from public.swarm_214",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_214" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_214Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_214_get",
    "Fetch one row from public.swarm_214 by id",
    public_swarm_214Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_214" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_214Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_215_list",
    "List rows from public.swarm_215",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_215" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_215Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_215_get",
    "Fetch one row from public.swarm_215 by id",
    public_swarm_215Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_215" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_215Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_216_list",
    "List rows from public.swarm_216",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_216" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_216Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_216_get",
    "Fetch one row from public.swarm_216 by id",
    public_swarm_216Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_216" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_216Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_217_list",
    "List rows from public.swarm_217",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_217" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_217Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_217_get",
    "Fetch one row from public.swarm_217 by id",
    public_swarm_217Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_217" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_217Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_218_list",
    "List rows from public.swarm_218",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_218" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_218Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_218_get",
    "Fetch one row from public.swarm_218 by id",
    public_swarm_218Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_218" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_218Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_219_list",
    "List rows from public.swarm_219",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_219" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_219Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_219_get",
    "Fetch one row from public.swarm_219 by id",
    public_swarm_219Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_219" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_219Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_22_list",
    "List rows from public.swarm_22",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_22" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_22Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_22_get",
    "Fetch one row from public.swarm_22 by id",
    public_swarm_22Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_22" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_22Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_220_list",
    "List rows from public.swarm_220",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_220" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_220Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_220_get",
    "Fetch one row from public.swarm_220 by id",
    public_swarm_220Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_220" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_220Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_221_list",
    "List rows from public.swarm_221",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_221" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_221Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_221_get",
    "Fetch one row from public.swarm_221 by id",
    public_swarm_221Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_221" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_221Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_222_list",
    "List rows from public.swarm_222",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_222" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_222Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_222_get",
    "Fetch one row from public.swarm_222 by id",
    public_swarm_222Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_222" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_222Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_223_list",
    "List rows from public.swarm_223",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_223" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_223Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_223_get",
    "Fetch one row from public.swarm_223 by id",
    public_swarm_223Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_223" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_223Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_224_list",
    "List rows from public.swarm_224",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_224" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_224Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_224_get",
    "Fetch one row from public.swarm_224 by id",
    public_swarm_224Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_224" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_224Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_225_list",
    "List rows from public.swarm_225",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_225" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_225Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_225_get",
    "Fetch one row from public.swarm_225 by id",
    public_swarm_225Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_225" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_225Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_226_list",
    "List rows from public.swarm_226",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_226" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_226Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_226_get",
    "Fetch one row from public.swarm_226 by id",
    public_swarm_226Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_226" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_226Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_227_list",
    "List rows from public.swarm_227",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_227" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_227Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_227_get",
    "Fetch one row from public.swarm_227 by id",
    public_swarm_227Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_227" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_227Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_228_list",
    "List rows from public.swarm_228",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_228" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_228Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_228_get",
    "Fetch one row from public.swarm_228 by id",
    public_swarm_228Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_228" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_228Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_229_list",
    "List rows from public.swarm_229",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_229" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_229Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_229_get",
    "Fetch one row from public.swarm_229 by id",
    public_swarm_229Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_229" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_229Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_23_list",
    "List rows from public.swarm_23",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_23" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_23Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_23_get",
    "Fetch one row from public.swarm_23 by id",
    public_swarm_23Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_23" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_23Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_230_list",
    "List rows from public.swarm_230",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_230" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_230Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_230_get",
    "Fetch one row from public.swarm_230 by id",
    public_swarm_230Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_230" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_230Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_231_list",
    "List rows from public.swarm_231",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_231" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_231Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_231_get",
    "Fetch one row from public.swarm_231 by id",
    public_swarm_231Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_231" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_231Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_232_list",
    "List rows from public.swarm_232",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_232" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_232Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_232_get",
    "Fetch one row from public.swarm_232 by id",
    public_swarm_232Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_232" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_232Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_233_list",
    "List rows from public.swarm_233",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_233" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_233Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_233_get",
    "Fetch one row from public.swarm_233 by id",
    public_swarm_233Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_233" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_233Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_234_list",
    "List rows from public.swarm_234",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_234" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_234Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_234_get",
    "Fetch one row from public.swarm_234 by id",
    public_swarm_234Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_234" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_234Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_235_list",
    "List rows from public.swarm_235",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_235" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_235Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_235_get",
    "Fetch one row from public.swarm_235 by id",
    public_swarm_235Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_235" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_235Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_236_list",
    "List rows from public.swarm_236",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_236" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_236Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_236_get",
    "Fetch one row from public.swarm_236 by id",
    public_swarm_236Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_236" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_236Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_237_list",
    "List rows from public.swarm_237",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_237" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_237Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_237_get",
    "Fetch one row from public.swarm_237 by id",
    public_swarm_237Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_237" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_237Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_238_list",
    "List rows from public.swarm_238",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_238" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_238Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_238_get",
    "Fetch one row from public.swarm_238 by id",
    public_swarm_238Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_238" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_238Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_239_list",
    "List rows from public.swarm_239",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_239" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_239Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_239_get",
    "Fetch one row from public.swarm_239 by id",
    public_swarm_239Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_239" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_239Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_24_list",
    "List rows from public.swarm_24",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_24" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_24Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_24_get",
    "Fetch one row from public.swarm_24 by id",
    public_swarm_24Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_24" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_24Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_240_list",
    "List rows from public.swarm_240",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_240" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_240Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_240_get",
    "Fetch one row from public.swarm_240 by id",
    public_swarm_240Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_240" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_240Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_241_list",
    "List rows from public.swarm_241",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_241" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_241Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_241_get",
    "Fetch one row from public.swarm_241 by id",
    public_swarm_241Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_241" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_241Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_242_list",
    "List rows from public.swarm_242",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_242" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_242Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_242_get",
    "Fetch one row from public.swarm_242 by id",
    public_swarm_242Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_242" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_242Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_243_list",
    "List rows from public.swarm_243",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_243" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_243Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_243_get",
    "Fetch one row from public.swarm_243 by id",
    public_swarm_243Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_243" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_243Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_244_list",
    "List rows from public.swarm_244",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_244" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_244Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_244_get",
    "Fetch one row from public.swarm_244 by id",
    public_swarm_244Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_244" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_244Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_245_list",
    "List rows from public.swarm_245",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_245" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_245Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_245_get",
    "Fetch one row from public.swarm_245 by id",
    public_swarm_245Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_245" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_245Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_246_list",
    "List rows from public.swarm_246",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_246" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_246Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_246_get",
    "Fetch one row from public.swarm_246 by id",
    public_swarm_246Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_246" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_246Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_247_list",
    "List rows from public.swarm_247",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_247" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_247Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_247_get",
    "Fetch one row from public.swarm_247 by id",
    public_swarm_247Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_247" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_247Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_248_list",
    "List rows from public.swarm_248",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_248" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_248Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_248_get",
    "Fetch one row from public.swarm_248 by id",
    public_swarm_248Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_248" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_248Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_249_list",
    "List rows from public.swarm_249",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_249" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_249Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_249_get",
    "Fetch one row from public.swarm_249 by id",
    public_swarm_249Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_249" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_249Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_25_list",
    "List rows from public.swarm_25",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_25" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_25Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_25_get",
    "Fetch one row from public.swarm_25 by id",
    public_swarm_25Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_25" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_25Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_250_list",
    "List rows from public.swarm_250",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_250" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_250Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_250_get",
    "Fetch one row from public.swarm_250 by id",
    public_swarm_250Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_250" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_250Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_251_list",
    "List rows from public.swarm_251",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_251" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_251Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_251_get",
    "Fetch one row from public.swarm_251 by id",
    public_swarm_251Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_251" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_251Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_252_list",
    "List rows from public.swarm_252",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_252" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_252Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_252_get",
    "Fetch one row from public.swarm_252 by id",
    public_swarm_252Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_252" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_252Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_253_list",
    "List rows from public.swarm_253",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_253" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_253Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_253_get",
    "Fetch one row from public.swarm_253 by id",
    public_swarm_253Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_253" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_253Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_254_list",
    "List rows from public.swarm_254",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_254" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_254Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_254_get",
    "Fetch one row from public.swarm_254 by id",
    public_swarm_254Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_254" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_254Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_255_list",
    "List rows from public.swarm_255",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_255" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_255Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_255_get",
    "Fetch one row from public.swarm_255 by id",
    public_swarm_255Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_255" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_255Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_256_list",
    "List rows from public.swarm_256",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_256" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_256Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_256_get",
    "Fetch one row from public.swarm_256 by id",
    public_swarm_256Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_256" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_256Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_257_list",
    "List rows from public.swarm_257",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_257" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_257Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_257_get",
    "Fetch one row from public.swarm_257 by id",
    public_swarm_257Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_257" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_257Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_258_list",
    "List rows from public.swarm_258",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_258" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_258Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_258_get",
    "Fetch one row from public.swarm_258 by id",
    public_swarm_258Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_258" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_258Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_259_list",
    "List rows from public.swarm_259",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_259" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_259Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_259_get",
    "Fetch one row from public.swarm_259 by id",
    public_swarm_259Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_259" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_259Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_26_list",
    "List rows from public.swarm_26",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_26" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_26Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_26_get",
    "Fetch one row from public.swarm_26 by id",
    public_swarm_26Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_26" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_26Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_260_list",
    "List rows from public.swarm_260",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_260" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_260Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_260_get",
    "Fetch one row from public.swarm_260 by id",
    public_swarm_260Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_260" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_260Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_261_list",
    "List rows from public.swarm_261",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_261" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_261Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_261_get",
    "Fetch one row from public.swarm_261 by id",
    public_swarm_261Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_261" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_261Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_262_list",
    "List rows from public.swarm_262",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_262" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_262Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_262_get",
    "Fetch one row from public.swarm_262 by id",
    public_swarm_262Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_262" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_262Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_263_list",
    "List rows from public.swarm_263",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_263" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_263Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_263_get",
    "Fetch one row from public.swarm_263 by id",
    public_swarm_263Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_263" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_263Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_264_list",
    "List rows from public.swarm_264",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_264" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_264Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_264_get",
    "Fetch one row from public.swarm_264 by id",
    public_swarm_264Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_264" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_264Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_265_list",
    "List rows from public.swarm_265",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_265" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_265Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_265_get",
    "Fetch one row from public.swarm_265 by id",
    public_swarm_265Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_265" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_265Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_266_list",
    "List rows from public.swarm_266",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_266" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_266Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_266_get",
    "Fetch one row from public.swarm_266 by id",
    public_swarm_266Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_266" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_266Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_267_list",
    "List rows from public.swarm_267",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_267" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_267Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_267_get",
    "Fetch one row from public.swarm_267 by id",
    public_swarm_267Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_267" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_267Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_268_list",
    "List rows from public.swarm_268",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_268" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_268Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_268_get",
    "Fetch one row from public.swarm_268 by id",
    public_swarm_268Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_268" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_268Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_269_list",
    "List rows from public.swarm_269",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_269" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_269Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_269_get",
    "Fetch one row from public.swarm_269 by id",
    public_swarm_269Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_269" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_269Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_27_list",
    "List rows from public.swarm_27",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_27" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_27Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_27_get",
    "Fetch one row from public.swarm_27 by id",
    public_swarm_27Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_27" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_27Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_270_list",
    "List rows from public.swarm_270",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_270" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_270Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_270_get",
    "Fetch one row from public.swarm_270 by id",
    public_swarm_270Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_270" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_270Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_271_list",
    "List rows from public.swarm_271",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_271" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_271Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_271_get",
    "Fetch one row from public.swarm_271 by id",
    public_swarm_271Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_271" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_271Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_272_list",
    "List rows from public.swarm_272",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_272" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_272Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_272_get",
    "Fetch one row from public.swarm_272 by id",
    public_swarm_272Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_272" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_272Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_273_list",
    "List rows from public.swarm_273",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_273" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_273Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_273_get",
    "Fetch one row from public.swarm_273 by id",
    public_swarm_273Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_273" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_273Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_274_list",
    "List rows from public.swarm_274",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_274" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_274Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_274_get",
    "Fetch one row from public.swarm_274 by id",
    public_swarm_274Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_274" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_274Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_275_list",
    "List rows from public.swarm_275",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_275" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_275Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_275_get",
    "Fetch one row from public.swarm_275 by id",
    public_swarm_275Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_275" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_275Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_276_list",
    "List rows from public.swarm_276",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_276" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_276Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_276_get",
    "Fetch one row from public.swarm_276 by id",
    public_swarm_276Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_276" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_276Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_277_list",
    "List rows from public.swarm_277",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_277" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_277Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_277_get",
    "Fetch one row from public.swarm_277 by id",
    public_swarm_277Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_277" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_277Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_278_list",
    "List rows from public.swarm_278",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_278" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_278Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_278_get",
    "Fetch one row from public.swarm_278 by id",
    public_swarm_278Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_278" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_278Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_279_list",
    "List rows from public.swarm_279",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_279" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_279Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_279_get",
    "Fetch one row from public.swarm_279 by id",
    public_swarm_279Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_279" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_279Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_28_list",
    "List rows from public.swarm_28",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_28" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_28Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_28_get",
    "Fetch one row from public.swarm_28 by id",
    public_swarm_28Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_28" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_28Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_280_list",
    "List rows from public.swarm_280",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_280" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_280Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_280_get",
    "Fetch one row from public.swarm_280 by id",
    public_swarm_280Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_280" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_280Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_281_list",
    "List rows from public.swarm_281",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_281" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_281Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_281_get",
    "Fetch one row from public.swarm_281 by id",
    public_swarm_281Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_281" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_281Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_282_list",
    "List rows from public.swarm_282",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_282" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_282Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_282_get",
    "Fetch one row from public.swarm_282 by id",
    public_swarm_282Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_282" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_282Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_283_list",
    "List rows from public.swarm_283",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_283" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_283Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_283_get",
    "Fetch one row from public.swarm_283 by id",
    public_swarm_283Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_283" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_283Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_284_list",
    "List rows from public.swarm_284",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_284" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_284Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_284_get",
    "Fetch one row from public.swarm_284 by id",
    public_swarm_284Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_284" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_284Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_285_list",
    "List rows from public.swarm_285",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_285" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_285Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_285_get",
    "Fetch one row from public.swarm_285 by id",
    public_swarm_285Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_285" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_285Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_286_list",
    "List rows from public.swarm_286",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_286" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_286Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_286_get",
    "Fetch one row from public.swarm_286 by id",
    public_swarm_286Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_286" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_286Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_287_list",
    "List rows from public.swarm_287",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_287" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_287Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_287_get",
    "Fetch one row from public.swarm_287 by id",
    public_swarm_287Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_287" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_287Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_288_list",
    "List rows from public.swarm_288",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_288" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_288Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_288_get",
    "Fetch one row from public.swarm_288 by id",
    public_swarm_288Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_288" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_288Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_289_list",
    "List rows from public.swarm_289",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_289" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_289Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_289_get",
    "Fetch one row from public.swarm_289 by id",
    public_swarm_289Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_289" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_289Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_29_list",
    "List rows from public.swarm_29",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_29" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_29Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_29_get",
    "Fetch one row from public.swarm_29 by id",
    public_swarm_29Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_29" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_29Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_290_list",
    "List rows from public.swarm_290",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_290" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_290Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_290_get",
    "Fetch one row from public.swarm_290 by id",
    public_swarm_290Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_290" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_290Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_291_list",
    "List rows from public.swarm_291",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_291" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_291Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_291_get",
    "Fetch one row from public.swarm_291 by id",
    public_swarm_291Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_291" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_291Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_292_list",
    "List rows from public.swarm_292",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_292" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_292Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_292_get",
    "Fetch one row from public.swarm_292 by id",
    public_swarm_292Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_292" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_292Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_293_list",
    "List rows from public.swarm_293",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_293" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_293Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_293_get",
    "Fetch one row from public.swarm_293 by id",
    public_swarm_293Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_293" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_293Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_294_list",
    "List rows from public.swarm_294",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_294" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_294Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_294_get",
    "Fetch one row from public.swarm_294 by id",
    public_swarm_294Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_294" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_294Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_295_list",
    "List rows from public.swarm_295",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_295" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_295Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_295_get",
    "Fetch one row from public.swarm_295 by id",
    public_swarm_295Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_295" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_295Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_296_list",
    "List rows from public.swarm_296",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_296" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_296Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_296_get",
    "Fetch one row from public.swarm_296 by id",
    public_swarm_296Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_296" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_296Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_297_list",
    "List rows from public.swarm_297",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_297" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_297Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_297_get",
    "Fetch one row from public.swarm_297 by id",
    public_swarm_297Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_297" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_297Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_298_list",
    "List rows from public.swarm_298",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_298" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_298Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_298_get",
    "Fetch one row from public.swarm_298 by id",
    public_swarm_298Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_298" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_298Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_299_list",
    "List rows from public.swarm_299",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_299" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_299Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_299_get",
    "Fetch one row from public.swarm_299 by id",
    public_swarm_299Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_299" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_299Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_3_list",
    "List rows from public.swarm_3",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_3" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_3Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_3_get",
    "Fetch one row from public.swarm_3 by id",
    public_swarm_3Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_3" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_3Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_30_list",
    "List rows from public.swarm_30",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_30" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_30Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_30_get",
    "Fetch one row from public.swarm_30 by id",
    public_swarm_30Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_30" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_30Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_300_list",
    "List rows from public.swarm_300",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_300" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_300Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_300_get",
    "Fetch one row from public.swarm_300 by id",
    public_swarm_300Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_300" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_300Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_31_list",
    "List rows from public.swarm_31",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_31" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_31Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_31_get",
    "Fetch one row from public.swarm_31 by id",
    public_swarm_31Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_31" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_31Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_32_list",
    "List rows from public.swarm_32",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_32" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_32Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_32_get",
    "Fetch one row from public.swarm_32 by id",
    public_swarm_32Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_32" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_32Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_33_list",
    "List rows from public.swarm_33",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_33" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_33Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_33_get",
    "Fetch one row from public.swarm_33 by id",
    public_swarm_33Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_33" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_33Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_34_list",
    "List rows from public.swarm_34",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_34" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_34Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_34_get",
    "Fetch one row from public.swarm_34 by id",
    public_swarm_34Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_34" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_34Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_35_list",
    "List rows from public.swarm_35",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_35" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_35Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_35_get",
    "Fetch one row from public.swarm_35 by id",
    public_swarm_35Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_35" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_35Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_36_list",
    "List rows from public.swarm_36",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_36" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_36Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_36_get",
    "Fetch one row from public.swarm_36 by id",
    public_swarm_36Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_36" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_36Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_37_list",
    "List rows from public.swarm_37",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_37" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_37Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_37_get",
    "Fetch one row from public.swarm_37 by id",
    public_swarm_37Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_37" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_37Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_38_list",
    "List rows from public.swarm_38",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_38" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_38Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_38_get",
    "Fetch one row from public.swarm_38 by id",
    public_swarm_38Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_38" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_38Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_39_list",
    "List rows from public.swarm_39",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_39" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_39Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_39_get",
    "Fetch one row from public.swarm_39 by id",
    public_swarm_39Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_39" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_39Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_4_list",
    "List rows from public.swarm_4",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_4" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_4Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_4_get",
    "Fetch one row from public.swarm_4 by id",
    public_swarm_4Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_4" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_4Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_40_list",
    "List rows from public.swarm_40",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_40" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_40Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_40_get",
    "Fetch one row from public.swarm_40 by id",
    public_swarm_40Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_40" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_40Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_41_list",
    "List rows from public.swarm_41",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_41" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_41Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_41_get",
    "Fetch one row from public.swarm_41 by id",
    public_swarm_41Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_41" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_41Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_42_list",
    "List rows from public.swarm_42",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_42" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_42Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_42_get",
    "Fetch one row from public.swarm_42 by id",
    public_swarm_42Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_42" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_42Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_43_list",
    "List rows from public.swarm_43",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_43" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_43Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_43_get",
    "Fetch one row from public.swarm_43 by id",
    public_swarm_43Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_43" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_43Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_44_list",
    "List rows from public.swarm_44",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_44" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_44Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_44_get",
    "Fetch one row from public.swarm_44 by id",
    public_swarm_44Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_44" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_44Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_45_list",
    "List rows from public.swarm_45",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_45" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_45Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_45_get",
    "Fetch one row from public.swarm_45 by id",
    public_swarm_45Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_45" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_45Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_46_list",
    "List rows from public.swarm_46",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_46" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_46Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_46_get",
    "Fetch one row from public.swarm_46 by id",
    public_swarm_46Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_46" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_46Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_47_list",
    "List rows from public.swarm_47",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_47" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_47Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_47_get",
    "Fetch one row from public.swarm_47 by id",
    public_swarm_47Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_47" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_47Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_48_list",
    "List rows from public.swarm_48",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_48" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_48Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_48_get",
    "Fetch one row from public.swarm_48 by id",
    public_swarm_48Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_48" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_48Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_49_list",
    "List rows from public.swarm_49",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_49" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_49Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_49_get",
    "Fetch one row from public.swarm_49 by id",
    public_swarm_49Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_49" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_49Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_5_list",
    "List rows from public.swarm_5",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_5" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_5Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_5_get",
    "Fetch one row from public.swarm_5 by id",
    public_swarm_5Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_5" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_5Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_50_list",
    "List rows from public.swarm_50",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_50" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_50Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_50_get",
    "Fetch one row from public.swarm_50 by id",
    public_swarm_50Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_50" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_50Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_51_list",
    "List rows from public.swarm_51",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_51" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_51Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_51_get",
    "Fetch one row from public.swarm_51 by id",
    public_swarm_51Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_51" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_51Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_52_list",
    "List rows from public.swarm_52",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_52" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_52Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_52_get",
    "Fetch one row from public.swarm_52 by id",
    public_swarm_52Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_52" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_52Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_53_list",
    "List rows from public.swarm_53",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_53" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_53Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_53_get",
    "Fetch one row from public.swarm_53 by id",
    public_swarm_53Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_53" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_53Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_54_list",
    "List rows from public.swarm_54",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_54" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_54Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_54_get",
    "Fetch one row from public.swarm_54 by id",
    public_swarm_54Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_54" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_54Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_55_list",
    "List rows from public.swarm_55",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_55" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_55Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_55_get",
    "Fetch one row from public.swarm_55 by id",
    public_swarm_55Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_55" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_55Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_56_list",
    "List rows from public.swarm_56",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_56" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_56Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_56_get",
    "Fetch one row from public.swarm_56 by id",
    public_swarm_56Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_56" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_56Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_57_list",
    "List rows from public.swarm_57",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_57" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_57Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_57_get",
    "Fetch one row from public.swarm_57 by id",
    public_swarm_57Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_57" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_57Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_58_list",
    "List rows from public.swarm_58",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_58" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_58Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_58_get",
    "Fetch one row from public.swarm_58 by id",
    public_swarm_58Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_58" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_58Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_59_list",
    "List rows from public.swarm_59",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_59" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_59Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_59_get",
    "Fetch one row from public.swarm_59 by id",
    public_swarm_59Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_59" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_59Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_6_list",
    "List rows from public.swarm_6",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_6" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_6Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_6_get",
    "Fetch one row from public.swarm_6 by id",
    public_swarm_6Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_6" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_6Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_60_list",
    "List rows from public.swarm_60",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_60" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_60Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_60_get",
    "Fetch one row from public.swarm_60 by id",
    public_swarm_60Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_60" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_60Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_61_list",
    "List rows from public.swarm_61",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_61" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_61Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_61_get",
    "Fetch one row from public.swarm_61 by id",
    public_swarm_61Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_61" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_61Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_62_list",
    "List rows from public.swarm_62",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_62" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_62Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_62_get",
    "Fetch one row from public.swarm_62 by id",
    public_swarm_62Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_62" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_62Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_63_list",
    "List rows from public.swarm_63",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_63" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_63Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_63_get",
    "Fetch one row from public.swarm_63 by id",
    public_swarm_63Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_63" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_63Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_64_list",
    "List rows from public.swarm_64",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_64" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_64Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_64_get",
    "Fetch one row from public.swarm_64 by id",
    public_swarm_64Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_64" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_64Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_65_list",
    "List rows from public.swarm_65",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_65" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_65Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_65_get",
    "Fetch one row from public.swarm_65 by id",
    public_swarm_65Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_65" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_65Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_66_list",
    "List rows from public.swarm_66",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_66" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_66Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_66_get",
    "Fetch one row from public.swarm_66 by id",
    public_swarm_66Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_66" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_66Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_67_list",
    "List rows from public.swarm_67",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_67" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_67Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_67_get",
    "Fetch one row from public.swarm_67 by id",
    public_swarm_67Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_67" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_67Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_68_list",
    "List rows from public.swarm_68",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_68" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_68Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_68_get",
    "Fetch one row from public.swarm_68 by id",
    public_swarm_68Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_68" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_68Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_69_list",
    "List rows from public.swarm_69",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_69" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_69Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_69_get",
    "Fetch one row from public.swarm_69 by id",
    public_swarm_69Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_69" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_69Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_7_list",
    "List rows from public.swarm_7",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_7" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_7Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_7_get",
    "Fetch one row from public.swarm_7 by id",
    public_swarm_7Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_7" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_7Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_70_list",
    "List rows from public.swarm_70",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_70" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_70Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_70_get",
    "Fetch one row from public.swarm_70 by id",
    public_swarm_70Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_70" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_70Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_71_list",
    "List rows from public.swarm_71",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_71" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_71Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_71_get",
    "Fetch one row from public.swarm_71 by id",
    public_swarm_71Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_71" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_71Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_72_list",
    "List rows from public.swarm_72",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_72" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_72Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_72_get",
    "Fetch one row from public.swarm_72 by id",
    public_swarm_72Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_72" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_72Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_73_list",
    "List rows from public.swarm_73",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_73" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_73Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_73_get",
    "Fetch one row from public.swarm_73 by id",
    public_swarm_73Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_73" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_73Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_74_list",
    "List rows from public.swarm_74",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_74" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_74Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_74_get",
    "Fetch one row from public.swarm_74 by id",
    public_swarm_74Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_74" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_74Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_75_list",
    "List rows from public.swarm_75",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_75" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_75Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_75_get",
    "Fetch one row from public.swarm_75 by id",
    public_swarm_75Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_75" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_75Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_76_list",
    "List rows from public.swarm_76",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_76" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_76Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_76_get",
    "Fetch one row from public.swarm_76 by id",
    public_swarm_76Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_76" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_76Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_77_list",
    "List rows from public.swarm_77",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_77" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_77Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_77_get",
    "Fetch one row from public.swarm_77 by id",
    public_swarm_77Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_77" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_77Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_78_list",
    "List rows from public.swarm_78",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_78" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_78Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_78_get",
    "Fetch one row from public.swarm_78 by id",
    public_swarm_78Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_78" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_78Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_79_list",
    "List rows from public.swarm_79",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_79" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_79Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_79_get",
    "Fetch one row from public.swarm_79 by id",
    public_swarm_79Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_79" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_79Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_8_list",
    "List rows from public.swarm_8",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_8" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_8Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_8_get",
    "Fetch one row from public.swarm_8 by id",
    public_swarm_8Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_8" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_8Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_80_list",
    "List rows from public.swarm_80",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_80" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_80Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_80_get",
    "Fetch one row from public.swarm_80 by id",
    public_swarm_80Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_80" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_80Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_81_list",
    "List rows from public.swarm_81",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_81" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_81Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_81_get",
    "Fetch one row from public.swarm_81 by id",
    public_swarm_81Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_81" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_81Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_82_list",
    "List rows from public.swarm_82",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_82" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_82Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_82_get",
    "Fetch one row from public.swarm_82 by id",
    public_swarm_82Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_82" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_82Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_83_list",
    "List rows from public.swarm_83",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_83" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_83Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_83_get",
    "Fetch one row from public.swarm_83 by id",
    public_swarm_83Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_83" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_83Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_84_list",
    "List rows from public.swarm_84",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_84" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_84Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_84_get",
    "Fetch one row from public.swarm_84 by id",
    public_swarm_84Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_84" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_84Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_85_list",
    "List rows from public.swarm_85",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_85" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_85Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_85_get",
    "Fetch one row from public.swarm_85 by id",
    public_swarm_85Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_85" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_85Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_86_list",
    "List rows from public.swarm_86",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_86" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_86Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_86_get",
    "Fetch one row from public.swarm_86 by id",
    public_swarm_86Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_86" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_86Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_87_list",
    "List rows from public.swarm_87",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_87" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_87Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_87_get",
    "Fetch one row from public.swarm_87 by id",
    public_swarm_87Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_87" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_87Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_88_list",
    "List rows from public.swarm_88",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_88" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_88Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_88_get",
    "Fetch one row from public.swarm_88 by id",
    public_swarm_88Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_88" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_88Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_89_list",
    "List rows from public.swarm_89",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_89" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_89Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_89_get",
    "Fetch one row from public.swarm_89 by id",
    public_swarm_89Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_89" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_89Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_9_list",
    "List rows from public.swarm_9",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_9" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_9Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_9_get",
    "Fetch one row from public.swarm_9 by id",
    public_swarm_9Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_9" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_9Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_90_list",
    "List rows from public.swarm_90",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_90" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_90Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_90_get",
    "Fetch one row from public.swarm_90 by id",
    public_swarm_90Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_90" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_90Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_91_list",
    "List rows from public.swarm_91",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_91" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_91Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_91_get",
    "Fetch one row from public.swarm_91 by id",
    public_swarm_91Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_91" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_91Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_92_list",
    "List rows from public.swarm_92",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_92" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_92Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_92_get",
    "Fetch one row from public.swarm_92 by id",
    public_swarm_92Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_92" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_92Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_93_list",
    "List rows from public.swarm_93",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_93" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_93Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_93_get",
    "Fetch one row from public.swarm_93 by id",
    public_swarm_93Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_93" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_93Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_94_list",
    "List rows from public.swarm_94",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_94" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_94Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_94_get",
    "Fetch one row from public.swarm_94 by id",
    public_swarm_94Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_94" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_94Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_95_list",
    "List rows from public.swarm_95",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_95" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_95Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_95_get",
    "Fetch one row from public.swarm_95 by id",
    public_swarm_95Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_95" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_95Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_96_list",
    "List rows from public.swarm_96",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_96" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_96Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_96_get",
    "Fetch one row from public.swarm_96 by id",
    public_swarm_96Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_96" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_96Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_97_list",
    "List rows from public.swarm_97",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_97" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_97Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_97_get",
    "Fetch one row from public.swarm_97 by id",
    public_swarm_97Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_97" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_97Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_98_list",
    "List rows from public.swarm_98",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_98" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_98Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_98_get",
    "Fetch one row from public.swarm_98 by id",
    public_swarm_98Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_98" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_98Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_99_list",
    "List rows from public.swarm_99",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_99" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_swarm_99Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_swarm_99_get",
    "Fetch one row from public.swarm_99 by id",
    public_swarm_99Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."swarm_99" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_swarm_99Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_tele_list",
    "List rows from public.tele",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."tele" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_teleRow.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_users_list",
    "List rows from public.users",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."users" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_usersRow.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_users_get",
    "Fetch one row from public.users by id",
    public_usersRow.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."users" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_usersRow.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_users__drop_table_users___list",
    "List rows from public.users; drop table users--",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."users; drop table users--" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_users__drop_table_users__Row.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_users__drop_table_users___get",
    "Fetch one row from public.users; drop table users-- by id",
    public_users__drop_table_users__Row.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."users; drop table users--" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_users__drop_table_users__Row.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  server.tool(
    "public_with_dropped_list",
    "List rows from public.with_dropped",
    { limit: z.number().int().min(1).max(100).optional() },
    async ({ limit }) => {
      const rows = await sql.unsafe('select * from "public"."with_dropped" limit ' + Math.min(limit ?? 20, 100));
      const parsed = rows.map((row) => public_with_droppedRow.parse(row));
      return { content: [{ type: "text", text: JSON.stringify(parsed, null, 2) }] };
    },
  );
  server.tool(
    "public_with_dropped_get",
    "Fetch one row from public.with_dropped by id",
    public_with_droppedRow.pick({ "id": true }).shape,
    async (input) => {
      const rows = await sql.unsafe('select * from "public"."with_dropped" where "id" = $1 limit 1', [input["id"]]);
      const row = rows.length > 0 ? public_with_droppedRow.parse(rows[0]) : null;
      return { content: [{ type: "text", text: JSON.stringify(row, null, 2) }] };
    },
  );
  return server;
}
