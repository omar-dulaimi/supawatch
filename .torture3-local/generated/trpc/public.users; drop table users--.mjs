// Generated by supawatch. Do not edit.
import { public_users__drop_table_users__Row, public_users__drop_table_users__Insert } from "../zod/public.users; drop table users--.mjs";

export function createPublic_users__drop_table_users__Router(t, sql) {
  return t.router({
    list: t.procedure.query(async () => {
      const rows = await sql.unsafe('select * from "public"."users; drop table users--" limit 100');
      return rows.map((row) => public_users__drop_table_users__Row.parse(row));
    }),
    byId: t.procedure.input(public_users__drop_table_users__Row.pick({ "id": true })).query(async ({ input }) => {
      const rows = await sql.unsafe('select * from "public"."users; drop table users--" where "id" = $1 limit 1', [input["id"]]);
      return rows.length > 0 ? public_users__drop_table_users__Row.parse(rows[0]) : null;
    }),
    create: t.procedure.input(public_users__drop_table_users__Insert).mutation(async ({ input }) => {
      const keys = Object.keys(input);
      const cols = keys.map((k) => '"' + k.replace(/"/g, '""') + '"').join(', ');
      const params = keys.map((_, i) => '$' + (i + 1)).join(', ');
      const rows = await sql.unsafe('insert into "public"."users; drop table users--" (' + cols + ') values (' + params + ') returning *', keys.map((k) => input[k]));
      return public_users__drop_table_users__Row.parse(rows[0]);
    }),
  });
}
