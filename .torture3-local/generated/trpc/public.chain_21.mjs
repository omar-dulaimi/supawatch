// Generated by supawatch. Do not edit.
import { public_chain_21Row, public_chain_21Insert } from "../zod/public.chain_21.mjs";

export function createPublic_chain_21Router(t, sql) {
  return t.router({
    list: t.procedure.query(async () => {
      const rows = await sql.unsafe('select * from "public"."chain_21" limit 100');
      return rows.map((row) => public_chain_21Row.parse(row));
    }),
    byId: t.procedure.input(public_chain_21Row.pick({ "id": true })).query(async ({ input }) => {
      const rows = await sql.unsafe('select * from "public"."chain_21" where "id" = $1 limit 1', [input["id"]]);
      return rows.length > 0 ? public_chain_21Row.parse(rows[0]) : null;
    }),
    create: t.procedure.input(public_chain_21Insert).mutation(async ({ input }) => {
      const keys = Object.keys(input);
      const cols = keys.map((k) => '"' + k.replace(/"/g, '""') + '"').join(', ');
      const params = keys.map((_, i) => '$' + (i + 1)).join(', ');
      const rows = await sql.unsafe('insert into "public"."chain_21" (' + cols + ') values (' + params + ') returning *', keys.map((k) => input[k]));
      return public_chain_21Row.parse(rows[0]);
    }),
  });
}
