// Generated by supawatch. Do not edit.
import { public_swarm_39Row, public_swarm_39Insert } from "../zod/public.swarm_39.mjs";

export function createPublic_swarm_39Router(t, sql) {
  return t.router({
    list: t.procedure.query(async () => {
      const rows = await sql.unsafe('select * from "public"."swarm_39" limit 100');
      return rows.map((row) => public_swarm_39Row.parse(row));
    }),
    byId: t.procedure.input(public_swarm_39Row.pick({ "id": true })).query(async ({ input }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_39" where "id" = $1 limit 1', [input["id"]]);
      return rows.length > 0 ? public_swarm_39Row.parse(rows[0]) : null;
    }),
    create: t.procedure.input(public_swarm_39Insert).mutation(async ({ input }) => {
      const keys = Object.keys(input);
      const cols = keys.map((k) => '"' + k.replace(/"/g, '""') + '"').join(', ');
      const params = keys.map((_, i) => '$' + (i + 1)).join(', ');
      const rows = await sql.unsafe('insert into "public"."swarm_39" (' + cols + ') values (' + params + ') returning *', keys.map((k) => input[k]));
      return public_swarm_39Row.parse(rows[0]);
    }),
  });
}
