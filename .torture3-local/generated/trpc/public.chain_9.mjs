// Generated by supawatch. Do not edit.
import { public_chain_9Row, public_chain_9Insert } from "../zod/public.chain_9.mjs";

export function createPublic_chain_9Router(t, sql) {
  return t.router({
    list: t.procedure.query(async () => {
      const rows = await sql.unsafe('select * from "public"."chain_9" limit 100');
      return rows.map((row) => public_chain_9Row.parse(row));
    }),
    byId: t.procedure.input(public_chain_9Row.pick({ "id": true })).query(async ({ input }) => {
      const rows = await sql.unsafe('select * from "public"."chain_9" where "id" = $1 limit 1', [input["id"]]);
      return rows.length > 0 ? public_chain_9Row.parse(rows[0]) : null;
    }),
    create: t.procedure.input(public_chain_9Insert).mutation(async ({ input }) => {
      const keys = Object.keys(input);
      const cols = keys.map((k) => '"' + k.replace(/"/g, '""') + '"').join(', ');
      const params = keys.map((_, i) => '$' + (i + 1)).join(', ');
      const rows = await sql.unsafe('insert into "public"."chain_9" (' + cols + ') values (' + params + ') returning *', keys.map((k) => input[k]));
      return public_chain_9Row.parse(rows[0]);
    }),
  });
}
