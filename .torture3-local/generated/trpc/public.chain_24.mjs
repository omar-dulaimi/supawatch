// Generated by supawatch. Do not edit.
import { public_chain_24Row, public_chain_24Insert } from "../zod/public.chain_24.mjs";

export function createPublic_chain_24Router(t, sql) {
  return t.router({
    list: t.procedure.query(async () => {
      const rows = await sql.unsafe('select * from "public"."chain_24" limit 100');
      return rows.map((row) => public_chain_24Row.parse(row));
    }),
    byId: t.procedure.input(public_chain_24Row.pick({ "id": true })).query(async ({ input }) => {
      const rows = await sql.unsafe('select * from "public"."chain_24" where "id" = $1 limit 1', [input["id"]]);
      return rows.length > 0 ? public_chain_24Row.parse(rows[0]) : null;
    }),
    create: t.procedure.input(public_chain_24Insert).mutation(async ({ input }) => {
      const keys = Object.keys(input);
      const cols = keys.map((k) => '"' + k.replace(/"/g, '""') + '"').join(', ');
      const params = keys.map((_, i) => '$' + (i + 1)).join(', ');
      const rows = await sql.unsafe('insert into "public"."chain_24" (' + cols + ') values (' + params + ') returning *', keys.map((k) => input[k]));
      return public_chain_24Row.parse(rows[0]);
    }),
  });
}
