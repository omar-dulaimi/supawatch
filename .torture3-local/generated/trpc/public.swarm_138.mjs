// Generated by supawatch. Do not edit.
import { public_swarm_138Row, public_swarm_138Insert } from "../zod/public.swarm_138.mjs";

export function createPublic_swarm_138Router(t, sql) {
  return t.router({
    list: t.procedure.query(async () => {
      const rows = await sql.unsafe('select * from "public"."swarm_138" limit 100');
      return rows.map((row) => public_swarm_138Row.parse(row));
    }),
    byId: t.procedure.input(public_swarm_138Row.pick({ "id": true })).query(async ({ input }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_138" where "id" = $1 limit 1', [input["id"]]);
      return rows.length > 0 ? public_swarm_138Row.parse(rows[0]) : null;
    }),
    create: t.procedure.input(public_swarm_138Insert).mutation(async ({ input }) => {
      const keys = Object.keys(input);
      const cols = keys.map((k) => '"' + k.replace(/"/g, '""') + '"').join(', ');
      const params = keys.map((_, i) => '$' + (i + 1)).join(', ');
      const rows = await sql.unsafe('insert into "public"."swarm_138" (' + cols + ') values (' + params + ') returning *', keys.map((k) => input[k]));
      return public_swarm_138Row.parse(rows[0]);
    }),
  });
}
