// Generated by supawatch. Do not edit.
import { public_swarm_69Row, public_swarm_69Insert } from "../zod/public.swarm_69.mjs";

export function createPublic_swarm_69Router(t, sql) {
  return t.router({
    list: t.procedure.query(async () => {
      const rows = await sql.unsafe('select * from "public"."swarm_69" limit 100');
      return rows.map((row) => public_swarm_69Row.parse(row));
    }),
    byId: t.procedure.input(public_swarm_69Row.pick({ "id": true })).query(async ({ input }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_69" where "id" = $1 limit 1', [input["id"]]);
      return rows.length > 0 ? public_swarm_69Row.parse(rows[0]) : null;
    }),
    create: t.procedure.input(public_swarm_69Insert).mutation(async ({ input }) => {
      const keys = Object.keys(input);
      const cols = keys.map((k) => '"' + k.replace(/"/g, '""') + '"').join(', ');
      const params = keys.map((_, i) => '$' + (i + 1)).join(', ');
      const rows = await sql.unsafe('insert into "public"."swarm_69" (' + cols + ') values (' + params + ') returning *', keys.map((k) => input[k]));
      return public_swarm_69Row.parse(rows[0]);
    }),
  });
}
