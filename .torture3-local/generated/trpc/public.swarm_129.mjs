// Generated by supawatch. Do not edit.
import { public_swarm_129Row, public_swarm_129Insert } from "../zod/public.swarm_129.mjs";

export function createPublic_swarm_129Router(t, sql) {
  return t.router({
    list: t.procedure.query(async () => {
      const rows = await sql.unsafe('select * from "public"."swarm_129" limit 100');
      return rows.map((row) => public_swarm_129Row.parse(row));
    }),
    byId: t.procedure.input(public_swarm_129Row.pick({ "id": true })).query(async ({ input }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_129" where "id" = $1 limit 1', [input["id"]]);
      return rows.length > 0 ? public_swarm_129Row.parse(rows[0]) : null;
    }),
    create: t.procedure.input(public_swarm_129Insert).mutation(async ({ input }) => {
      const keys = Object.keys(input);
      const cols = keys.map((k) => '"' + k.replace(/"/g, '""') + '"').join(', ');
      const params = keys.map((_, i) => '$' + (i + 1)).join(', ');
      const rows = await sql.unsafe('insert into "public"."swarm_129" (' + cols + ') values (' + params + ') returning *', keys.map((k) => input[k]));
      return public_swarm_129Row.parse(rows[0]);
    }),
  });
}
