// Generated by supawatch. Do not edit.
import { public_swarm_217Row, public_swarm_217Insert } from "../zod/public.swarm_217.mjs";

export function createPublic_swarm_217Router(t, sql) {
  return t.router({
    list: t.procedure.query(async () => {
      const rows = await sql.unsafe('select * from "public"."swarm_217" limit 100');
      return rows.map((row) => public_swarm_217Row.parse(row));
    }),
    byId: t.procedure.input(public_swarm_217Row.pick({ "id": true })).query(async ({ input }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_217" where "id" = $1 limit 1', [input["id"]]);
      return rows.length > 0 ? public_swarm_217Row.parse(rows[0]) : null;
    }),
    create: t.procedure.input(public_swarm_217Insert).mutation(async ({ input }) => {
      const keys = Object.keys(input);
      const cols = keys.map((k) => '"' + k.replace(/"/g, '""') + '"').join(', ');
      const params = keys.map((_, i) => '$' + (i + 1)).join(', ');
      const rows = await sql.unsafe('insert into "public"."swarm_217" (' + cols + ') values (' + params + ') returning *', keys.map((k) => input[k]));
      return public_swarm_217Row.parse(rows[0]);
    }),
  });
}
