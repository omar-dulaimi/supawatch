// Generated by supawatch. Do not edit.
import { public_swarm_20Row, public_swarm_20Insert } from "../zod/public.swarm_20.mjs";

export function createPublic_swarm_20Router(t, sql) {
  return t.router({
    list: t.procedure.query(async () => {
      const rows = await sql.unsafe('select * from "public"."swarm_20" limit 100');
      return rows.map((row) => public_swarm_20Row.parse(row));
    }),
    byId: t.procedure.input(public_swarm_20Row.pick({ "id": true })).query(async ({ input }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_20" where "id" = $1 limit 1', [input["id"]]);
      return rows.length > 0 ? public_swarm_20Row.parse(rows[0]) : null;
    }),
    create: t.procedure.input(public_swarm_20Insert).mutation(async ({ input }) => {
      const keys = Object.keys(input);
      const cols = keys.map((k) => '"' + k.replace(/"/g, '""') + '"').join(', ');
      const params = keys.map((_, i) => '$' + (i + 1)).join(', ');
      const rows = await sql.unsafe('insert into "public"."swarm_20" (' + cols + ') values (' + params + ') returning *', keys.map((k) => input[k]));
      return public_swarm_20Row.parse(rows[0]);
    }),
  });
}
