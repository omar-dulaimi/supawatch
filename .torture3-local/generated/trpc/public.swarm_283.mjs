// Generated by supawatch. Do not edit.
import { public_swarm_283Row, public_swarm_283Insert } from "../zod/public.swarm_283.mjs";

export function createPublic_swarm_283Router(t, sql) {
  return t.router({
    list: t.procedure.query(async () => {
      const rows = await sql.unsafe('select * from "public"."swarm_283" limit 100');
      return rows.map((row) => public_swarm_283Row.parse(row));
    }),
    byId: t.procedure.input(public_swarm_283Row.pick({ "id": true })).query(async ({ input }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_283" where "id" = $1 limit 1', [input["id"]]);
      return rows.length > 0 ? public_swarm_283Row.parse(rows[0]) : null;
    }),
    create: t.procedure.input(public_swarm_283Insert).mutation(async ({ input }) => {
      const keys = Object.keys(input);
      const cols = keys.map((k) => '"' + k.replace(/"/g, '""') + '"').join(', ');
      const params = keys.map((_, i) => '$' + (i + 1)).join(', ');
      const rows = await sql.unsafe('insert into "public"."swarm_283" (' + cols + ') values (' + params + ') returning *', keys.map((k) => input[k]));
      return public_swarm_283Row.parse(rows[0]);
    }),
  });
}
