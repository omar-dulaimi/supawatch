// Generated by supawatch. Do not edit.
import { public_swarm_142Row, public_swarm_142Insert } from "../zod/public.swarm_142.mjs";

export function createPublic_swarm_142Router(t, sql) {
  return t.router({
    list: t.procedure.query(async () => {
      const rows = await sql.unsafe('select * from "public"."swarm_142" limit 100');
      return rows.map((row) => public_swarm_142Row.parse(row));
    }),
    byId: t.procedure.input(public_swarm_142Row.pick({ "id": true })).query(async ({ input }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_142" where "id" = $1 limit 1', [input["id"]]);
      return rows.length > 0 ? public_swarm_142Row.parse(rows[0]) : null;
    }),
    create: t.procedure.input(public_swarm_142Insert).mutation(async ({ input }) => {
      const keys = Object.keys(input);
      const cols = keys.map((k) => '"' + k.replace(/"/g, '""') + '"').join(', ');
      const params = keys.map((_, i) => '$' + (i + 1)).join(', ');
      const rows = await sql.unsafe('insert into "public"."swarm_142" (' + cols + ') values (' + params + ') returning *', keys.map((k) => input[k]));
      return public_swarm_142Row.parse(rows[0]);
    }),
  });
}
