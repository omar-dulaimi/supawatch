// Generated by supawatch. Do not edit.
import { public_extremesRow, public_extremesInsert } from "../zod/public.extremes.mjs";

export function createPublic_extremesRouter(t, sql) {
  return t.router({
    list: t.procedure.query(async () => {
      const rows = await sql.unsafe('select * from "public"."extremes" limit 100');
      return rows.map((row) => public_extremesRow.parse(row));
    }),
    byId: t.procedure.input(public_extremesRow.pick({ "id": true })).query(async ({ input }) => {
      const rows = await sql.unsafe('select * from "public"."extremes" where "id" = $1 limit 1', [input["id"]]);
      return rows.length > 0 ? public_extremesRow.parse(rows[0]) : null;
    }),
    create: t.procedure.input(public_extremesInsert).mutation(async ({ input }) => {
      const keys = Object.keys(input);
      const cols = keys.map((k) => '"' + k.replace(/"/g, '""') + '"').join(', ');
      const params = keys.map((_, i) => '$' + (i + 1)).join(', ');
      const rows = await sql.unsafe('insert into "public"."extremes" (' + cols + ') values (' + params + ') returning *', keys.map((k) => input[k]));
      return public_extremesRow.parse(rows[0]);
    }),
  });
}
