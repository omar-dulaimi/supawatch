// Generated by supawatch. Do not edit.
import { public_swarm_117Row, public_swarm_117Insert } from "../zod/public.swarm_117.mjs";

export function createPublic_swarm_117Router(t, sql) {
  return t.router({
    list: t.procedure.query(async () => {
      const rows = await sql.unsafe('select * from "public"."swarm_117" limit 100');
      return rows.map((row) => public_swarm_117Row.parse(row));
    }),
    byId: t.procedure.input(public_swarm_117Row.pick({ "id": true })).query(async ({ input }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_117" where "id" = $1 limit 1', [input["id"]]);
      return rows.length > 0 ? public_swarm_117Row.parse(rows[0]) : null;
    }),
    create: t.procedure.input(public_swarm_117Insert).mutation(async ({ input }) => {
      const keys = Object.keys(input);
      const cols = keys.map((k) => '"' + k.replace(/"/g, '""') + '"').join(', ');
      const params = keys.map((_, i) => '$' + (i + 1)).join(', ');
      const rows = await sql.unsafe('insert into "public"."swarm_117" (' + cols + ') values (' + params + ') returning *', keys.map((k) => input[k]));
      return public_swarm_117Row.parse(rows[0]);
    }),
  });
}
