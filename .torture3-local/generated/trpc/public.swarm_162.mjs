// Generated by supawatch. Do not edit.
import { public_swarm_162Row, public_swarm_162Insert } from "../zod/public.swarm_162.mjs";

export function createPublic_swarm_162Router(t, sql) {
  return t.router({
    list: t.procedure.query(async () => {
      const rows = await sql.unsafe('select * from "public"."swarm_162" limit 100');
      return rows.map((row) => public_swarm_162Row.parse(row));
    }),
    byId: t.procedure.input(public_swarm_162Row.pick({ "id": true })).query(async ({ input }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_162" where "id" = $1 limit 1', [input["id"]]);
      return rows.length > 0 ? public_swarm_162Row.parse(rows[0]) : null;
    }),
    create: t.procedure.input(public_swarm_162Insert).mutation(async ({ input }) => {
      const keys = Object.keys(input);
      const cols = keys.map((k) => '"' + k.replace(/"/g, '""') + '"').join(', ');
      const params = keys.map((_, i) => '$' + (i + 1)).join(', ');
      const rows = await sql.unsafe('insert into "public"."swarm_162" (' + cols + ') values (' + params + ') returning *', keys.map((k) => input[k]));
      return public_swarm_162Row.parse(rows[0]);
    }),
  });
}
