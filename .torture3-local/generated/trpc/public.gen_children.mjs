// Generated by supawatch. Do not edit.
import { public_gen_childrenRow, public_gen_childrenInsert } from "../zod/public.gen_children.mjs";

export function createPublic_gen_childrenRouter(t, sql) {
  return t.router({
    list: t.procedure.query(async () => {
      const rows = await sql.unsafe('select * from "public"."gen_children" limit 100');
      return rows.map((row) => public_gen_childrenRow.parse(row));
    }),
    byId: t.procedure.input(public_gen_childrenRow.pick({ "id": true })).query(async ({ input }) => {
      const rows = await sql.unsafe('select * from "public"."gen_children" where "id" = $1 limit 1', [input["id"]]);
      return rows.length > 0 ? public_gen_childrenRow.parse(rows[0]) : null;
    }),
    create: t.procedure.input(public_gen_childrenInsert).mutation(async ({ input }) => {
      const keys = Object.keys(input);
      const cols = keys.map((k) => '"' + k.replace(/"/g, '""') + '"').join(', ');
      const params = keys.map((_, i) => '$' + (i + 1)).join(', ');
      const rows = await sql.unsafe('insert into "public"."gen_children" (' + cols + ') values (' + params + ') returning *', keys.map((k) => input[k]));
      return public_gen_childrenRow.parse(rows[0]);
    }),
  });
}
