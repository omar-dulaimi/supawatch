// Generated by supawatch. Do not edit.
import { public_chain_17Row, public_chain_17Insert } from "../zod/public.chain_17.mjs";

export function createPublic_chain_17Router(t, sql) {
  return t.router({
    list: t.procedure.query(async () => {
      const rows = await sql.unsafe('select * from "public"."chain_17" limit 100');
      return rows.map((row) => public_chain_17Row.parse(row));
    }),
    byId: t.procedure.input(public_chain_17Row.pick({ "id": true })).query(async ({ input }) => {
      const rows = await sql.unsafe('select * from "public"."chain_17" where "id" = $1 limit 1', [input["id"]]);
      return rows.length > 0 ? public_chain_17Row.parse(rows[0]) : null;
    }),
    create: t.procedure.input(public_chain_17Insert).mutation(async ({ input }) => {
      const keys = Object.keys(input);
      const cols = keys.map((k) => '"' + k.replace(/"/g, '""') + '"').join(', ');
      const params = keys.map((_, i) => '$' + (i + 1)).join(', ');
      const rows = await sql.unsafe('insert into "public"."chain_17" (' + cols + ') values (' + params + ') returning *', keys.map((k) => input[k]));
      return public_chain_17Row.parse(rows[0]);
    }),
  });
}
