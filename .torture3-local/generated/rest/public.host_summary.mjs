// Generated by supawatch. Do not edit.
import { Hono } from "hono";
import { public_host_summaryRow } from "../zod/public.host_summary.mjs";

export function createPublic_host_summaryRoutes(sql) {
  const app = new Hono();
  app.get("/", async (c) => {
    const rows = await sql.unsafe('select * from "public"."host_summary" limit 100');
    return c.json(rows.map((row) => public_host_summaryRow.parse(row)));
  });
  return app;
}
