// Generated by supawatch. Do not edit.
import { Hono } from "hono";
import { public_swarm_295Row, public_swarm_295Insert } from "../zod/public.swarm_295.mjs";

export function createPublic_swarm_295Routes(sql) {
  const app = new Hono();
  app.get("/", async (c) => {
    const rows = await sql.unsafe('select * from "public"."swarm_295" limit 100');
    return c.json(rows.map((row) => public_swarm_295Row.parse(row)));
  });
  app.get("/:id", async (c) => {
    const rows = await sql.unsafe('select * from "public"."swarm_295" where "id" = $1 limit 1', [c.req.param("id")]);
    if (rows.length === 0) return c.json({ error: "not found" }, 404);
    return c.json(public_swarm_295Row.parse(rows[0]));
  });
  app.post("/", async (c) => {
    const body = await c.req.json();
    const parsed = public_swarm_295Insert.safeParse(body);
    if (!parsed.success) {
      return c.json({ error: parsed.error.issues[0]?.message ?? "invalid" }, 400);
    }
    const keys = Object.keys(parsed.data);
    const cols = keys.map((k) => '"' + k.replace(/"/g, '""') + '"').join(', ');
    const params = keys.map((_, i) => '$' + (i + 1)).join(', ');
    const rows = await sql.unsafe('insert into "public"."swarm_295" (' + cols + ') values (' + params + ') returning *', keys.map((k) => parsed.data[k]));
    return c.json(public_swarm_295Row.parse(rows[0]), 201);
  });
  return app;
}
