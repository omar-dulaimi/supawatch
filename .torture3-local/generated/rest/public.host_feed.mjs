// Generated by supawatch. Do not edit.
import { Hono } from "hono";
import { public_host_feedRow } from "../zod/public.host_feed.mjs";

export function createPublic_host_feedRoutes(sql) {
  const app = new Hono();
  app.get("/", async (c) => {
    const rows = await sql.unsafe('select * from "public"."host_feed" limit 100');
    return c.json(rows.map((row) => public_host_feedRow.parse(row)));
  });
  return app;
}
