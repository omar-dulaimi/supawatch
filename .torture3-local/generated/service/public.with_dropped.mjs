// Generated by supawatch. Do not edit.
import { public_with_droppedRow, public_with_droppedInsert, public_with_droppedUpdate } from "../zod/public.with_dropped.mjs";

export function createPublic_with_droppedRepo(sql) {
  return {
    async list({ limit = 100, offset = 0 } = {}) {
      const rows = await sql.unsafe('select * from "public"."with_dropped" limit $1 offset $2', [Math.min(limit, 1000), offset]);
      return rows.map((row) => public_with_droppedRow.parse(row));
    },
    async findById(id) {
      const rows = await sql.unsafe('select * from "public"."with_dropped" where "id" = $1 limit 1', [id]);
      return rows.length > 0 ? public_with_droppedRow.parse(rows[0]) : null;
    },
    async create(input) {
      const data = public_with_droppedInsert.parse(input);
      const keys = Object.keys(data);
      const cols = keys.map((k) => '"' + k.replace(/"/g, '""') + '"').join(', ');
      const params = keys.map((_, i) => '$' + (i + 1)).join(', ');
      const rows = await sql.unsafe('insert into "public"."with_dropped" (' + cols + ') values (' + params + ') returning *', keys.map((k) => data[k]));
      return public_with_droppedRow.parse(rows[0]);
    },
    async update(id, patch) {
      const data = public_with_droppedUpdate.parse(patch);
      const keys = Object.keys(data);
      if (keys.length === 0) return this.findById(id);
      const sets = keys.map((k, i) => '"' + k.replace(/"/g, '""') + '" = $' + (i + 2)).join(', ');
      const rows = await sql.unsafe('update "public"."with_dropped" set ' + sets + ' where "id" = $1 returning *', [id, ...keys.map((k) => data[k])]);
      return rows.length > 0 ? public_with_droppedRow.parse(rows[0]) : null;
    },
    async remove(id) {
      const rows = await sql.unsafe('delete from "public"."with_dropped" where "id" = $1 returning "id"', [id]);
      return rows.length > 0;
    },
  };
}
