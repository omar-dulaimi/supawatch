// Generated by supawatch. Do not edit.
import { public_swarm_6Row, public_swarm_6Insert, public_swarm_6Update } from "../zod/public.swarm_6.mjs";

export function createPublic_swarm_6Repo(sql) {
  return {
    async list({ limit = 100, offset = 0 } = {}) {
      const rows = await sql.unsafe('select * from "public"."swarm_6" limit $1 offset $2', [Math.min(limit, 1000), offset]);
      return rows.map((row) => public_swarm_6Row.parse(row));
    },
    async findById(id) {
      const rows = await sql.unsafe('select * from "public"."swarm_6" where "id" = $1 limit 1', [id]);
      return rows.length > 0 ? public_swarm_6Row.parse(rows[0]) : null;
    },
    async create(input) {
      const data = public_swarm_6Insert.parse(input);
      const keys = Object.keys(data);
      const cols = keys.map((k) => '"' + k.replace(/"/g, '""') + '"').join(', ');
      const params = keys.map((_, i) => '$' + (i + 1)).join(', ');
      const rows = await sql.unsafe('insert into "public"."swarm_6" (' + cols + ') values (' + params + ') returning *', keys.map((k) => data[k]));
      return public_swarm_6Row.parse(rows[0]);
    },
    async update(id, patch) {
      const data = public_swarm_6Update.parse(patch);
      const keys = Object.keys(data);
      if (keys.length === 0) return this.findById(id);
      const sets = keys.map((k, i) => '"' + k.replace(/"/g, '""') + '" = $' + (i + 2)).join(', ');
      const rows = await sql.unsafe('update "public"."swarm_6" set ' + sets + ' where "id" = $1 returning *', [id, ...keys.map((k) => data[k])]);
      return rows.length > 0 ? public_swarm_6Row.parse(rows[0]) : null;
    },
    async remove(id) {
      const rows = await sql.unsafe('delete from "public"."swarm_6" where "id" = $1 returning "id"', [id]);
      return rows.length > 0;
    },
  };
}
