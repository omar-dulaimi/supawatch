// Generated by supawatch. Do not edit.
import { public_teleRow, public_teleInsert, public_teleUpdate } from "../zod/public.tele.mjs";

export function createPublic_teleRepo(sql) {
  return {
    async list({ limit = 100, offset = 0 } = {}) {
      const rows = await sql.unsafe('select * from "public"."tele" limit $1 offset $2', [Math.min(limit, 1000), offset]);
      return rows.map((row) => public_teleRow.parse(row));
    },
    async create(input) {
      const data = public_teleInsert.parse(input);
      const keys = Object.keys(data);
      const cols = keys.map((k) => '"' + k.replace(/"/g, '""') + '"').join(', ');
      const params = keys.map((_, i) => '$' + (i + 1)).join(', ');
      const rows = await sql.unsafe('insert into "public"."tele" (' + cols + ') values (' + params + ') returning *', keys.map((k) => data[k]));
      return public_teleRow.parse(rows[0]);
    },
  };
}
