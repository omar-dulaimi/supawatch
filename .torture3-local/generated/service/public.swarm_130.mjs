// Generated by supawatch. Do not edit.
import { public_swarm_130Row, public_swarm_130Insert, public_swarm_130Update } from "../zod/public.swarm_130.mjs";

export function createPublic_swarm_130Repo(sql) {
  return {
    async list({ limit = 100, offset = 0 } = {}) {
      const rows = await sql.unsafe('select * from "public"."swarm_130" limit $1 offset $2', [Math.min(limit, 1000), offset]);
      return rows.map((row) => public_swarm_130Row.parse(row));
    },
    async findById(id) {
      const rows = await sql.unsafe('select * from "public"."swarm_130" where "id" = $1 limit 1', [id]);
      return rows.length > 0 ? public_swarm_130Row.parse(rows[0]) : null;
    },
    async create(input) {
      const data = public_swarm_130Insert.parse(input);
      const keys = Object.keys(data);
      const cols = keys.map((k) => '"' + k.replace(/"/g, '""') + '"').join(', ');
      const params = keys.map((_, i) => '$' + (i + 1)).join(', ');
      const rows = await sql.unsafe('insert into "public"."swarm_130" (' + cols + ') values (' + params + ') returning *', keys.map((k) => data[k]));
      return public_swarm_130Row.parse(rows[0]);
    },
    async update(id, patch) {
      const data = public_swarm_130Update.parse(patch);
      const keys = Object.keys(data);
      if (keys.length === 0) return this.findById(id);
      const sets = keys.map((k, i) => '"' + k.replace(/"/g, '""') + '" = $' + (i + 2)).join(', ');
      const rows = await sql.unsafe('update "public"."swarm_130" set ' + sets + ' where "id" = $1 returning *', [id, ...keys.map((k) => data[k])]);
      return rows.length > 0 ? public_swarm_130Row.parse(rows[0]) : null;
    },
    async remove(id) {
      const rows = await sql.unsafe('delete from "public"."swarm_130" where "id" = $1 returning "id"', [id]);
      return rows.length > 0;
    },
  };
}
