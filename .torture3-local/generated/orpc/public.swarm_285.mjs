// Generated by supawatch. Do not edit.
import { os } from "@orpc/server";
import { public_swarm_285Row, public_swarm_285Insert } from "../zod/public.swarm_285.mjs";

export function createPublic_swarm_285Orpc(sql) {
  const procedures = {
    list: os.handler(async () => {
      const rows = await sql.unsafe('select * from "public"."swarm_285" limit 100');
      return rows.map((row) => public_swarm_285Row.parse(row));
    }),
    byId: os.input(public_swarm_285Row.pick({ "id": true })).handler(async ({ input }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_285" where "id" = $1 limit 1', [input["id"]]);
      return rows.length > 0 ? public_swarm_285Row.parse(rows[0]) : null;
    }),
    create: os.input(public_swarm_285Insert).handler(async ({ input }) => {
      const keys = Object.keys(input);
      const cols = keys.map((k) => '"' + k.replace(/"/g, '""') + '"').join(', ');
      const params = keys.map((_, i) => '$' + (i + 1)).join(', ');
      const rows = await sql.unsafe('insert into "public"."swarm_285" (' + cols + ') values (' + params + ') returning *', keys.map((k) => input[k]));
      return public_swarm_285Row.parse(rows[0]);
    }),
  };
  return os.router(procedures);
}
