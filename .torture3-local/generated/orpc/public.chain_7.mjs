// Generated by supawatch. Do not edit.
import { os } from "@orpc/server";
import { public_chain_7Row, public_chain_7Insert } from "../zod/public.chain_7.mjs";

export function createPublic_chain_7Orpc(sql) {
  const procedures = {
    list: os.handler(async () => {
      const rows = await sql.unsafe('select * from "public"."chain_7" limit 100');
      return rows.map((row) => public_chain_7Row.parse(row));
    }),
    byId: os.input(public_chain_7Row.pick({ "id": true })).handler(async ({ input }) => {
      const rows = await sql.unsafe('select * from "public"."chain_7" where "id" = $1 limit 1', [input["id"]]);
      return rows.length > 0 ? public_chain_7Row.parse(rows[0]) : null;
    }),
    create: os.input(public_chain_7Insert).handler(async ({ input }) => {
      const keys = Object.keys(input);
      const cols = keys.map((k) => '"' + k.replace(/"/g, '""') + '"').join(', ');
      const params = keys.map((_, i) => '$' + (i + 1)).join(', ');
      const rows = await sql.unsafe('insert into "public"."chain_7" (' + cols + ') values (' + params + ') returning *', keys.map((k) => input[k]));
      return public_chain_7Row.parse(rows[0]);
    }),
  };
  return os.router(procedures);
}
