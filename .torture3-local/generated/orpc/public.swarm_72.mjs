// Generated by supawatch. Do not edit.
import { os } from "@orpc/server";
import { public_swarm_72Row, public_swarm_72Insert } from "../zod/public.swarm_72.mjs";

export function createPublic_swarm_72Orpc(sql) {
  const procedures = {
    list: os.handler(async () => {
      const rows = await sql.unsafe('select * from "public"."swarm_72" limit 100');
      return rows.map((row) => public_swarm_72Row.parse(row));
    }),
    byId: os.input(public_swarm_72Row.pick({ "id": true })).handler(async ({ input }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_72" where "id" = $1 limit 1', [input["id"]]);
      return rows.length > 0 ? public_swarm_72Row.parse(rows[0]) : null;
    }),
    create: os.input(public_swarm_72Insert).handler(async ({ input }) => {
      const keys = Object.keys(input);
      const cols = keys.map((k) => '"' + k.replace(/"/g, '""') + '"').join(', ');
      const params = keys.map((_, i) => '$' + (i + 1)).join(', ');
      const rows = await sql.unsafe('insert into "public"."swarm_72" (' + cols + ') values (' + params + ') returning *', keys.map((k) => input[k]));
      return public_swarm_72Row.parse(rows[0]);
    }),
  };
  return os.router(procedures);
}
