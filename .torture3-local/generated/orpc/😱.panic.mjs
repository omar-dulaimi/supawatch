// Generated by supawatch. Do not edit.
import { os } from "@orpc/server";
import { ___panicRow, ___panicInsert } from "../zod/😱.panic.mjs";

export function create___panicOrpc(sql) {
  const procedures = {
    list: os.handler(async () => {
      const rows = await sql.unsafe('select * from "😱"."panic" limit 100');
      return rows.map((row) => ___panicRow.parse(row));
    }),
    byId: os.input(___panicRow.pick({ "id": true })).handler(async ({ input }) => {
      const rows = await sql.unsafe('select * from "😱"."panic" where "id" = $1 limit 1', [input["id"]]);
      return rows.length > 0 ? ___panicRow.parse(rows[0]) : null;
    }),
    create: os.input(___panicInsert).handler(async ({ input }) => {
      const keys = Object.keys(input);
      const cols = keys.map((k) => '"' + k.replace(/"/g, '""') + '"').join(', ');
      const params = keys.map((_, i) => '$' + (i + 1)).join(', ');
      const rows = await sql.unsafe('insert into "😱"."panic" (' + cols + ') values (' + params + ') returning *', keys.map((k) => input[k]));
      return ___panicRow.parse(rows[0]);
    }),
  };
  return os.router(procedures);
}
