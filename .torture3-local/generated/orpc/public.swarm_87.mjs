// Generated by supawatch. Do not edit.
import { os } from "@orpc/server";
import { public_swarm_87Row, public_swarm_87Insert } from "../zod/public.swarm_87.mjs";

export function createPublic_swarm_87Orpc(sql) {
  const procedures = {
    list: os.handler(async () => {
      const rows = await sql.unsafe('select * from "public"."swarm_87" limit 100');
      return rows.map((row) => public_swarm_87Row.parse(row));
    }),
    byId: os.input(public_swarm_87Row.pick({ "id": true })).handler(async ({ input }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_87" where "id" = $1 limit 1', [input["id"]]);
      return rows.length > 0 ? public_swarm_87Row.parse(rows[0]) : null;
    }),
    create: os.input(public_swarm_87Insert).handler(async ({ input }) => {
      const keys = Object.keys(input);
      const cols = keys.map((k) => '"' + k.replace(/"/g, '""') + '"').join(', ');
      const params = keys.map((_, i) => '$' + (i + 1)).join(', ');
      const rows = await sql.unsafe('insert into "public"."swarm_87" (' + cols + ') values (' + params + ') returning *', keys.map((k) => input[k]));
      return public_swarm_87Row.parse(rows[0]);
    }),
  };
  return os.router(procedures);
}
