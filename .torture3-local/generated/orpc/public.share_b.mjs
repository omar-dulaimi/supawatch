// Generated by supawatch. Do not edit.
import { os } from "@orpc/server";
import { public_share_bRow, public_share_bInsert } from "../zod/public.share_b.mjs";

export function createPublic_share_bOrpc(sql) {
  const procedures = {
    list: os.handler(async () => {
      const rows = await sql.unsafe('select * from "public"."share_b" limit 100');
      return rows.map((row) => public_share_bRow.parse(row));
    }),
    byId: os.input(public_share_bRow.pick({ "id": true })).handler(async ({ input }) => {
      const rows = await sql.unsafe('select * from "public"."share_b" where "id" = $1 limit 1', [input["id"]]);
      return rows.length > 0 ? public_share_bRow.parse(rows[0]) : null;
    }),
    create: os.input(public_share_bInsert).handler(async ({ input }) => {
      const keys = Object.keys(input);
      const cols = keys.map((k) => '"' + k.replace(/"/g, '""') + '"').join(', ');
      const params = keys.map((_, i) => '$' + (i + 1)).join(', ');
      const rows = await sql.unsafe('insert into "public"."share_b" (' + cols + ') values (' + params + ') returning *', keys.map((k) => input[k]));
      return public_share_bRow.parse(rows[0]);
    }),
  };
  return os.router(procedures);
}
