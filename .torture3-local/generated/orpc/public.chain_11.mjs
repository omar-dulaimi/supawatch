// Generated by supawatch. Do not edit.
import { os } from "@orpc/server";
import { public_chain_11Row, public_chain_11Insert } from "../zod/public.chain_11.mjs";

export function createPublic_chain_11Orpc(sql) {
  const procedures = {
    list: os.handler(async () => {
      const rows = await sql.unsafe('select * from "public"."chain_11" limit 100');
      return rows.map((row) => public_chain_11Row.parse(row));
    }),
    byId: os.input(public_chain_11Row.pick({ "id": true })).handler(async ({ input }) => {
      const rows = await sql.unsafe('select * from "public"."chain_11" where "id" = $1 limit 1', [input["id"]]);
      return rows.length > 0 ? public_chain_11Row.parse(rows[0]) : null;
    }),
    create: os.input(public_chain_11Insert).handler(async ({ input }) => {
      const keys = Object.keys(input);
      const cols = keys.map((k) => '"' + k.replace(/"/g, '""') + '"').join(', ');
      const params = keys.map((_, i) => '$' + (i + 1)).join(', ');
      const rows = await sql.unsafe('insert into "public"."chain_11" (' + cols + ') values (' + params + ') returning *', keys.map((k) => input[k]));
      return public_chain_11Row.parse(rows[0]);
    }),
  };
  return os.router(procedures);
}
