// Generated by supawatch. Do not edit.
import { os } from "@orpc/server";
import { public_share_aRow, public_share_aInsert } from "../zod/public.share_a.mjs";

export function createPublic_share_aOrpc(sql) {
  const procedures = {
    list: os.handler(async () => {
      const rows = await sql.unsafe('select * from "public"."share_a" limit 100');
      return rows.map((row) => public_share_aRow.parse(row));
    }),
    byId: os.input(public_share_aRow.pick({ "id": true })).handler(async ({ input }) => {
      const rows = await sql.unsafe('select * from "public"."share_a" where "id" = $1 limit 1', [input["id"]]);
      return rows.length > 0 ? public_share_aRow.parse(rows[0]) : null;
    }),
    create: os.input(public_share_aInsert).handler(async ({ input }) => {
      const keys = Object.keys(input);
      const cols = keys.map((k) => '"' + k.replace(/"/g, '""') + '"').join(', ');
      const params = keys.map((_, i) => '$' + (i + 1)).join(', ');
      const rows = await sql.unsafe('insert into "public"."share_a" (' + cols + ') values (' + params + ') returning *', keys.map((k) => input[k]));
      return public_share_aRow.parse(rows[0]);
    }),
  };
  return os.router(procedures);
}
