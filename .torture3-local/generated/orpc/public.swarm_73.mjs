// Generated by supawatch. Do not edit.
import { os } from "@orpc/server";
import { public_swarm_73Row, public_swarm_73Insert } from "../zod/public.swarm_73.mjs";

export function createPublic_swarm_73Orpc(sql) {
  const procedures = {
    list: os.handler(async () => {
      const rows = await sql.unsafe('select * from "public"."swarm_73" limit 100');
      return rows.map((row) => public_swarm_73Row.parse(row));
    }),
    byId: os.input(public_swarm_73Row.pick({ "id": true })).handler(async ({ input }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_73" where "id" = $1 limit 1', [input["id"]]);
      return rows.length > 0 ? public_swarm_73Row.parse(rows[0]) : null;
    }),
    create: os.input(public_swarm_73Insert).handler(async ({ input }) => {
      const keys = Object.keys(input);
      const cols = keys.map((k) => '"' + k.replace(/"/g, '""') + '"').join(', ');
      const params = keys.map((_, i) => '$' + (i + 1)).join(', ');
      const rows = await sql.unsafe('insert into "public"."swarm_73" (' + cols + ') values (' + params + ') returning *', keys.map((k) => input[k]));
      return public_swarm_73Row.parse(rows[0]);
    }),
  };
  return os.router(procedures);
}
