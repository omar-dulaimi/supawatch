// Generated by supawatch. Do not edit.
import { os } from "@orpc/server";
import { public_swarm_45Row, public_swarm_45Insert } from "../zod/public.swarm_45.mjs";

export function createPublic_swarm_45Orpc(sql) {
  const procedures = {
    list: os.handler(async () => {
      const rows = await sql.unsafe('select * from "public"."swarm_45" limit 100');
      return rows.map((row) => public_swarm_45Row.parse(row));
    }),
    byId: os.input(public_swarm_45Row.pick({ "id": true })).handler(async ({ input }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_45" where "id" = $1 limit 1', [input["id"]]);
      return rows.length > 0 ? public_swarm_45Row.parse(rows[0]) : null;
    }),
    create: os.input(public_swarm_45Insert).handler(async ({ input }) => {
      const keys = Object.keys(input);
      const cols = keys.map((k) => '"' + k.replace(/"/g, '""') + '"').join(', ');
      const params = keys.map((_, i) => '$' + (i + 1)).join(', ');
      const rows = await sql.unsafe('insert into "public"."swarm_45" (' + cols + ') values (' + params + ') returning *', keys.map((k) => input[k]));
      return public_swarm_45Row.parse(rows[0]);
    }),
  };
  return os.router(procedures);
}
