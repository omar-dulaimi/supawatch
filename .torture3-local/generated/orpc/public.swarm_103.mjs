// Generated by supawatch. Do not edit.
import { os } from "@orpc/server";
import { public_swarm_103Row, public_swarm_103Insert } from "../zod/public.swarm_103.mjs";

export function createPublic_swarm_103Orpc(sql) {
  const procedures = {
    list: os.handler(async () => {
      const rows = await sql.unsafe('select * from "public"."swarm_103" limit 100');
      return rows.map((row) => public_swarm_103Row.parse(row));
    }),
    byId: os.input(public_swarm_103Row.pick({ "id": true })).handler(async ({ input }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_103" where "id" = $1 limit 1', [input["id"]]);
      return rows.length > 0 ? public_swarm_103Row.parse(rows[0]) : null;
    }),
    create: os.input(public_swarm_103Insert).handler(async ({ input }) => {
      const keys = Object.keys(input);
      const cols = keys.map((k) => '"' + k.replace(/"/g, '""') + '"').join(', ');
      const params = keys.map((_, i) => '$' + (i + 1)).join(', ');
      const rows = await sql.unsafe('insert into "public"."swarm_103" (' + cols + ') values (' + params + ') returning *', keys.map((k) => input[k]));
      return public_swarm_103Row.parse(rows[0]);
    }),
  };
  return os.router(procedures);
}
