// Generated by supawatch. Do not edit.
import { os } from "@orpc/server";
import { public_swarm_175Row, public_swarm_175Insert } from "../zod/public.swarm_175.mjs";

export function createPublic_swarm_175Orpc(sql) {
  const procedures = {
    list: os.handler(async () => {
      const rows = await sql.unsafe('select * from "public"."swarm_175" limit 100');
      return rows.map((row) => public_swarm_175Row.parse(row));
    }),
    byId: os.input(public_swarm_175Row.pick({ "id": true })).handler(async ({ input }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_175" where "id" = $1 limit 1', [input["id"]]);
      return rows.length > 0 ? public_swarm_175Row.parse(rows[0]) : null;
    }),
    create: os.input(public_swarm_175Insert).handler(async ({ input }) => {
      const keys = Object.keys(input);
      const cols = keys.map((k) => '"' + k.replace(/"/g, '""') + '"').join(', ');
      const params = keys.map((_, i) => '$' + (i + 1)).join(', ');
      const rows = await sql.unsafe('insert into "public"."swarm_175" (' + cols + ') values (' + params + ') returning *', keys.map((k) => input[k]));
      return public_swarm_175Row.parse(rows[0]);
    }),
  };
  return os.router(procedures);
}
