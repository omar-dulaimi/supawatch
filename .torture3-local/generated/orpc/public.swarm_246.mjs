// Generated by supawatch. Do not edit.
import { os } from "@orpc/server";
import { public_swarm_246Row, public_swarm_246Insert } from "../zod/public.swarm_246.mjs";

export function createPublic_swarm_246Orpc(sql) {
  const procedures = {
    list: os.handler(async () => {
      const rows = await sql.unsafe('select * from "public"."swarm_246" limit 100');
      return rows.map((row) => public_swarm_246Row.parse(row));
    }),
    byId: os.input(public_swarm_246Row.pick({ "id": true })).handler(async ({ input }) => {
      const rows = await sql.unsafe('select * from "public"."swarm_246" where "id" = $1 limit 1', [input["id"]]);
      return rows.length > 0 ? public_swarm_246Row.parse(rows[0]) : null;
    }),
    create: os.input(public_swarm_246Insert).handler(async ({ input }) => {
      const keys = Object.keys(input);
      const cols = keys.map((k) => '"' + k.replace(/"/g, '""') + '"').join(', ');
      const params = keys.map((_, i) => '$' + (i + 1)).join(', ');
      const rows = await sql.unsafe('insert into "public"."swarm_246" (' + cols + ') values (' + params + ') returning *', keys.map((k) => input[k]));
      return public_swarm_246Row.parse(rows[0]);
    }),
  };
  return os.router(procedures);
}
